 # QarzDaftar — Implementation Progress

Tracks progress against the MVP phases defined in `CLAUDE.md` section 44. Update this file as phases/tasks complete so progress survives across sessions.

## Phase 1 — Foundation ✅ DONE

- Next.js frontend scaffolded (TypeScript, Tailwind, App Router)
- NestJS backend scaffolded with full module layout (auth, users, debts, payments, reminders, notifications, push, telegram, ai, admin, database, common)
- Supabase client provider + hand-written `Database` types (`backend/src/database/database.types.ts`)
- DB schema migration `supabase/migrations/0001_init_schema.sql` (users, debts, payments, reminders, push_subscriptions, telegram_connections, notifications)
- Auth foundation: JWT strategy, global `JwtAuthGuard`/`RolesGuard`, `@Public()`/`@Roles()`/`@CurrentUser()` decorators, `/auth/*` stubs
- Basic app shell: root layout, Inter font, brand colors/Tailwind theme, `.env.example` for both apps

## Phase 2 — Core Debt ✅ DONE

- Backend: `UsersService` (find-or-create counterparty), `DebtsService`/`Controller` full CRUD with ownership checks, filters, sort, FK-embedded lender/borrower names
- Frontend: `/dashboard`, `/debts` (search/filter/sort), `/debts/new` (7-step add flow), `/debts/[id]` (detail/edit/delete), bottom nav

## Phase 3 — Payments ✅ DONE

- DB: `record_payment` Postgres function (migration `0002`) — atomic, row-locked, enforces ownership + non-negative remaining amount
- Backend: `GET/POST /debts/:debtId/payments`
- Frontend: payment history with progress bar, working "To'lov qo'shish" form

## Phase 4 — Confirmation ✅ DONE

- DB: `confirmation_token` column (migration `0003`)
- Backend: unauthenticated `GET/POST /debts/confirm/:token` (view + confirm/reject)
- Frontend: public `/confirm/[token]` page, "copy link" UI on debt detail

## Phase 5 — Notifications ✅ DONE

- DB: `push_enabled`/`telegram_enabled` prefs on `users` (migration `0004`, Telegram defaults OFF to avoid duplicate sends)
- Backend: Web Push (`web-push` + VAPID) subscribe/unsubscribe, Telegram bot `sendMessage`, `GET/PATCH /notifications`, `GET/POST /debts/:id/reminders`, `@nestjs/schedule` cron every 5 min processing due reminders
- Frontend: PWA manifest + `public/sw.js` (push, click, shell-only cache — never caches private pages), `/profile` (push + preference toggles), reminder picker on debt detail, `/activity` notifications list

**Known limitation:** none of Phases 2–5 could be tested live in a browser — login was still a stub, so every protected page only got build/lint verification.

## Phase 6 — Telegram Mini App 🔶 CODE-COMPLETE (live verification pending)

This phase delivers the **first real, working login** (previously `POST /auth/telegram` threw `NotImplementedException`). Once done, Phases 2–5 become testable end-to-end for real.

- [x] **Task 31** — Telegram `initData` verification (`backend/src/telegram/telegram-init-data.ts`): official HMAC-SHA256 algorithm, constant-time hash comparison, `auth_date` freshness check (max 24h). Builds clean.
- [x] **Task 32** — Real Telegram auth flow. DONE:
  - `TelegramService.findOrCreateUserByTelegramId(telegramId, name, username)` in `backend/src/telegram/telegram.service.ts` — looks up `telegram_connections` by `telegram_id`; if found, returns the linked user; otherwise creates a new `users` row + `telegram_connections` row.
  - `backend/src/auth/auth.service.ts`: `loginWithTelegram(initData)` → `verifyTelegramInitData` → find-or-create user → sign JWT (`{ sub, role }`) via `JwtService` → returns `{ accessToken, user }`.
  - `backend/src/auth/dto/telegram-auth.dto.ts`: `{ initData: string }` with `class-validator`.
  - `backend/src/auth/auth.controller.ts` `telegramLogin` now calls `AuthService.loginWithTelegram`.
  - `backend/src/auth/auth.module.ts`: imports `TelegramModule`, provides `AuthService`.
  - `npm run build` + `npm run lint` both clean in `backend/`.
- [x] **Task 33** — Frontend Mini App bootstrap. DONE:
  - `frontend/src/types/telegram.d.ts` — `window.Telegram.WebApp` global type (`initData`, `initDataUnsafe`, `themeParams`, `ready`/`expand`/`onEvent`/`offEvent`).
  - `frontend/src/lib/telegram-auth.ts` — `loginWithTelegramAction` Server Action, POSTs `initData` to `/auth/telegram` via `apiFetch`, sets the httpOnly `qd_session` cookie (`secure` gated on `NODE_ENV === "production"` so it still works when hitting a local dev backend over http).
  - `frontend/src/components/telegram-theme-sync.tsx` — client component mounted globally in root layout: calls `WebApp.ready()/expand()`, maps `themeParams` (`bg_color`/`text_color`/`secondary_bg_color`/`hint_color`/`button_color`) onto the app's existing CSS vars (`--background`/`--foreground`/`--card`/`--muted-foreground`/`--primary`), re-applies on `themeChanged`.
  - `frontend/src/components/telegram-bootstrap.tsx` — mounted on `/` only: detects `window.Telegram.WebApp.initData`, calls the login action once (`useRef` guard), `router.replace('/dashboard')` on success; no-ops silently outside Telegram (no `initData`).
  - `frontend/src/app/layout.tsx` — added `<Script src="https://telegram.org/js/telegram-web-app.js" strategy="beforeInteractive">` + `<TelegramThemeSync />`.
  - `frontend/src/app/page.tsx` — added `<TelegramBootstrap />`.
  - `npm run build` + `npm run lint` both clean in `frontend/`.

**Phase 6 is now code-complete** but still statically verified only (build/type-check/lint) — no live Supabase project or real Telegram bot token has been used to click through the flow in an actual Telegram client. That live verification is still owed before Phase 6 can be marked fully DONE.

**Deliberately out of scope for Phase 6** (per CLAUDE.md section 25 account linking needs an already-working phone/OTP login, which isn't built — no SMS provider chosen yet): merging a Telegram identity with an existing phone-created placeholder user by phone number. Telegram login always find-or-creates by `telegram_id` only.

## Phase 7 — AI Reminder ✅ DONE

Provider decision: **Google Gemini** (`gemini-2.5-flash`, called via raw REST `fetch` to `generativelanguage.googleapis.com` — no SDK dependency added), configured through the already-documented `AI_API_KEY` env var (no new env var name introduced).

- Backend (`backend/src/ai/`):
  - `entities/ai-reminder-tone.ts` — `AiReminderTone` union (`dostona`/`hurmatli`/`qisqa`/`rasmiy`/`hazilomuz`) + per-tone Uzbek prompt instructions.
  - `dto/generate-ai-reminder.dto.ts` — `{ debtId: UUID, tone }`.
  - `ai.service.ts` — `AiService.generateReminder(debt, currentUserId, tone)`: builds a prompt with lender/borrower names, remaining amount, due date, tone instruction, and an explicit hard rule ("never threaten/shame/insult/harass/pressure — always warm and respectful"); calls Gemini `generateContent`; throws `ServiceUnavailableException` if `AI_API_KEY` is unset, `InternalServerErrorException` on API/parse failure (Uzbek messages, never a raw error).
  - `ai.controller.ts` — `POST /ai/reminder` (matches the documented endpoint in CLAUDE.md §30 exactly): ownership-checked via `DebtsService.findOneForUser` before generation, so a user can only generate reminders for their own debts.
  - `ai.module.ts` — imports `DebtsModule`, wires `AiController`/`AiService`.
  - `npm run build` + `npm run lint` both clean in `backend/`.
- Frontend (debt detail page, `frontend/src/app/(app)/debts/[id]/`):
  - `ai-reminder-picker.tsx` — new client component: 5 tone buttons → generates via server action → shows the AI text in an **editable** `<textarea>` → "Nusxalash" (copy to clipboard) button.
  - No auto-send exists anywhere — this is intentional per CLAUDE.md §19 ("never automatically send an AI-generated message without explicit user confirmation"). QarzDaftar has no guaranteed channel to reach an arbitrary counterparty (no SMS provider, counterparty may not have linked Telegram), so the honest, non-fake MVP delivery mechanism is: user reviews/edits the AI text, then copies it and sends it themselves via whatever channel they choose (Telegram, SMS, WhatsApp). A real automatic in-app delivery channel (e.g. auto-send over the bot when the counterparty has Telegram linked) is a reasonable future enhancement, not built now.
  - `actions.ts` — added `generateAiReminderAction(debtId, tone)` server action (auth-gated like the file's other actions, returns `{ ok, message }`).
  - `lib/ai-api.ts` — new file, `generateAiReminder(token, { debtId, tone })` → `POST /ai/reminder`.
  - `lib/types.ts` — added `AiReminderTone`, `GenerateAiReminderInput`, `GenerateAiReminderResult`.
  - `page.tsx` — mounted `<AiReminderPicker debtId={debt.id} />` below the payment/reminder action row.
  - `npm run build` + `npm run lint` both clean in `frontend/`.
- `backend/.env.example` — annotated `AI_API_KEY` with a comment noting it's a Google Gemini key (https://ai.google.dev).

**Update (Phase 10 session)**: now tested live against a real `AI_API_KEY` — see Phase 10's manual checklist item 4. This surfaced and fixed real bugs (dead model name, insufficient token budget, markdown/quote leakage) that static verification alone had missed. `GEMINI_MODEL` is `gemini-flash-latest` and `maxOutputTokens` is `1024`, not `gemini-2.5-flash`/`200` as originally written above — this section is left as historical record of the original Phase 7 implementation; Phase 10 has the current state.

## Phase 8 — Statistics ✅ DONE (static verification only)

- Backend: new `backend/src/stats/` module (`stats.entity.ts` → `UserStats`, `stats.service.ts`, `stats.controller.ts`, `stats.module.ts`, registered in `app.module.ts`). `GET /stats` (JWT-protected like every other route, scoped to `@CurrentUser()`): fetches all debts where the user is lender or borrower (same `.or(lender_id.eq,borrower_id.eq)` pattern as `DebtsService.findAllForUser`), then computes in JS:
  - `totalGiven` — sum of `amount` where `lender_id === userId`
  - `totalTaken` — sum of `amount` where `borrower_id === userId`
  - `totalRepaid` — sum of `(amount - remaining_amount)` across all of the user's debts (both directions)
  - `totalRemaining` — sum of `remaining_amount` across all of the user's debts
  - `totalOverdue` — sum of `remaining_amount` for debts past `due_date` and not `paid`/`cancelled` (same overdue rule as the frontend's `isOverdue()` in `lib/summary.ts`)

  Note: `totalGiven + totalTaken === totalRepaid + totalRemaining` always holds (each debt's `amount` splits into its own repaid+remaining), matching the CLAUDE.md §18 worked example exactly.
- Frontend:
  - `lib/types.ts` — added `UserStats`; `lib/debts-api.ts` — added `getStats(token)` → `GET /stats`.
  - `components/summary-card.tsx` — widened `tone` from `"success" | "danger"` to also accept `"warning" | "primary"` (map-based lookup) so it could be reused for the statistics tiles instead of writing a new card component.
  - `components/stats-progress-bar.tsx` — new: a single simple stacked horizontal bar (repaid = success/green, remaining = warning/amber segments, rounded, `dataviz`-skill mark spec) with a text legend underneath (colored dot + label + `formatSom` amount). No hover/tooltip JS — all values are already shown as plain text in the stat tiles above, so a static bar was the appropriate "simple chart" per CLAUDE.md §18 ("do not overwhelm users") rather than an interactive analytics chart.
  - `app/(app)/statistics/page.tsx` — new page: server component following the exact same `getServerToken` → fetch → `SignInRequired`/`ErrorState` pattern as `dashboard`/`profile`. Renders the 5 CLAUDE.md §18 numbers as `SummaryCard`s (Jami bergan=success, Jami olgan=danger, Qaytarilgan=primary, Qolgan=warning, Muddati o'tgan=danger — reusing the same given=success/taken=danger color convention already established by `DebtCard`/dashboard) plus the `StatsProgressBar`. Empty state (`totalGiven + totalTaken === 0`) shows a short Uzbek message instead of a zeroed-out chart.
  - `app/(app)/profile/page.tsx` — added a "Statistika →" link card (statistics isn't one of the 5 fixed bottom-nav items per CLAUDE.md §38, so it's linked from profile instead).
- `npm run build` + `npm run lint` both clean in `backend/` and `frontend/`.

**Not tested live**: no live Supabase project is connected, so `GET /stats` has only been verified by reading the code and static build/lint — nobody has loaded `/statistics` in a real browser against real debt data yet.

**Do not repeat:** Phases 1–9 are done and verified per their sections above — do not re-implement or re-verify auth, debts CRUD, payments, confirmation, notifications/PWA, Telegram Mini App, AI reminders, statistics, or admin; only Phase 10 (Production) remains.

## Phase 9 — Admin ✅ DONE (static verification only)

- **Bootstrap note:** there is still no self-serve way to become an admin (correctly — a self-promotion endpoint would be a security hole). The *first* admin must be created by hand, e.g. `update users set role = 'admin' where id = '...'` directly in Supabase. From then on, existing admins can promote/demote other users from the `/admin/users` panel.
- Migration `0005_ai_reminder_logs.sql` — new `ai_reminder_logs` table (id, user_id, tone, created_at) so the "AI reminder usage" metric (CLAUDE.md §39) reflects real usage instead of being faked. `backend/src/ai/ai.service.ts` now injects `SUPABASE_CLIENT` and writes a best-effort log row after each successful Gemini generation (wrapped in try/catch — a logging failure never breaks the user-facing AI reminder feature). `ai.module.ts` now imports `DatabaseModule`.
- Backend: new `backend/src/admin/` module, registered in `app.module.ts`. `@Roles('admin')` on the controller class — combined with the already-global `RolesGuard`, this is real server-side authorization, not a frontend check (CLAUDE.md §8/32).
  - `GET /admin/stats` → `AdminStats` (totalUsers, newUsers [7d], activeUsers [proxy: distinct lender/borrower on a debt updated in the last 30 days — there's no login/last-seen tracking in the schema, documented in a code comment], totalDebts, paidDebts, overdueDebts, aiReminderUsage, pushSubscriptions, telegramConnectedUsers).
  - `GET /admin/users` → `AdminUserSummary[]` (id, name, phone, role, telegramConnected, createdAt) — **deliberately excludes all debt/financial data**, per CLAUDE.md §8 ("Admin private debt details'ni default holatda ko'rmasligi kerak").
  - `PATCH /admin/users/:id/role` → promote/demote a user; blocks an admin from changing their own role (`BadRequestException`). Note: because JWTs are stateless with no revocation mechanism (inherited from Phase 6), a role change only takes effect on the target's *next* login, not on their current session.
  - `GET /admin/reports` → `{ debtsByStatus, remindersByStatus }` — pure aggregate counts (all 6 debt statuses / 4 reminder statuses, zero-filled), never individual debts, amounts, or names — same privacy rule as `/admin/users`.
  - `npm run build` + `npm run lint` clean in `backend/`.
- Frontend: new top-level `frontend/src/app/admin/` route group (sibling to `(app)`, so it does **not** get the mobile `BottomNav` — desktop-first per CLAUDE.md §37/39). `admin/layout.tsx` gates on `getCurrentUser().role === 'admin'` (UX-only convenience; the real enforcement is the backend `RolesGuard` above) and renders a simple top nav (Boshqaruv paneli / Foydalanuvchilar / Hisobotlar / Profil).
  - `admin/page.tsx` — 9 `AdminStatTile` cards (new component — plain `toLocaleString` counts, not `formatSom` currency, since these are counts not money).
  - `admin/users/page.tsx` + `users/actions.ts` + `users/role-toggle-button.tsx` — responsive table (`overflow-x-auto`) of all users with a promote/demote button per row; disabled on the signed-in admin's own row (mirrors the backend's self-demotion guard).
  - `admin/reports/page.tsx` — two aggregate count tables (debts-by-status, reminders-by-status), reusing `statusLabel` and a new `reminderStatusLabel` in `lib/format.ts`.
  - `lib/admin-api.ts` (new) + `AdminStats`/`AdminUserSummary`/`AdminReports` types added to `lib/types.ts`.
  - `app/(app)/profile/page.tsx` — added an "Admin panel →" link, shown only when `user.role === 'admin'` (mirrors the existing Statistics link).
  - `public/sw.js` was checked: it only ever caches `/` (the shell) — `/admin/*` is never cached, consistent with CLAUDE.md §21.
  - `npm run build` + `npm run lint` clean in `frontend/`.

**Deliberate scope decisions** (documented rather than faked, matching the pattern set in Phases 6–7):
- **No "System Settings" screen.** CLAUDE.md §28's schema has no settings table and no concrete settings are specified anywhere else in the doc. Building toggles with nothing real behind them would be "fake functionality," which CLAUDE.md explicitly forbids. Skipped until real settings are specified.
- **No standalone "Analytics" screen.** Folded into the Dashboard's stat tiles — CLAUDE.md §39 doesn't distinguish what would be on a separate Analytics screen that isn't already one of the listed metrics.
- **No standalone "Notifications" (admin) screen.** Reminder delivery health is already covered by the Reports screen's `remindersByStatus` breakdown; a separate screen would just duplicate it.
- **"Admin Profile" reuses the existing `/profile` page** rather than a new page — an admin is a normal `users` row too, and `/profile` already shows name/phone/notification prefs. Only a link back into `/admin` was added.
- **"Reports" stays aggregate-only** (status counts, zero individual debts/amounts/names) — CLAUDE.md has no "user-submitted report/flag" data model, and building individual-debt drill-downs would violate the explicit privacy-first admin rule.

**Not tested live**: no live Supabase project is connected and no admin account exists yet (see bootstrap note above), so none of `/admin/*` has been loaded in a real browser — only static build/lint verification.

## Phase 10 — Production 🔶 IN PROGRESS (code-level hardening done; live deployment is a manual, user-driven step)

### Done this session — security audit + error handling (backend)

- **Fixed a real information-disclosure bug**: nearly every service (`debts`, `payments`, `users`, `telegram`, `notifications`, `stats`, `admin`, `reminders`, `push` — ~35 call sites) threw `InternalServerErrorException(error.message)`, which put the *raw* Supabase/Postgres error text straight into the HTTP response body — a direct violation of CLAUDE.md §41 ("Never show raw technical errors") and a minor info-disclosure risk (could leak table/column names to any caller). Rather than touching all ~35 call sites, added one central `backend/src/common/filters/all-exceptions.filter.ts` (`@Catch()`, wired via `app.useGlobalFilters()` in `main.ts`): it rewrites the message of any `InternalServerErrorException` specifically to a generic Uzbek string, logs the real error server-side via `Logger`, and leaves every other deliberately-authored `HttpException` (400/401/403/404/503/501, all already using safe Uzbek text) untouched. **Verified live**, not just statically: booted the compiled server with dummy env vars and confirmed via `curl` that a forced Supabase failure returns `{"statusCode":500,"message":"Serverda xatolik yuz berdi..."}"` to the client while the real `TypeError: fetch failed` shows up only in the server log.
- **Rate limiting was silently a no-op**: `ThrottlerModule.forRoot(...)` was imported in `app.module.ts` but `ThrottlerGuard` was never registered — so CLAUDE.md §32's "rate limiting where appropriate" wasn't actually enforced anywhere. Added `{ provide: APP_GUARD, useClass: ThrottlerGuard }`. Verified live via `curl` response headers (`X-RateLimit-Limit/Remaining/Reset` now present on every response).
- **CORS was fully open** (`app.enableCors()` with no options reflects any origin). Restricted to a configurable allow-list via new `CORS_ORIGINS` env var (comma-separated), defaulting to open only when unset (local dev convenience). Kept working for the one browser→backend call that actually needs cross-origin access: the public `/confirm/[token]` page's `confirm-actions.tsx` is a client component that calls the API directly from the browser (everything else goes through Next.js Server Actions, which aren't subject to CORS at all). Verified live: requests from an unlisted origin get no `Access-Control-Allow-Origin` header; the configured origin does.
- **Added `helmet`** (new dependency) via `app.use(helmet())` for standard security headers (CSP, HSTS, X-Frame-Options, etc.) — verified present in live response headers.
- **Health-check route was accidentally behind auth**: `GET /` (`AppController.getHello`) had no `@Public()`, so the global `JwtAuthGuard` returned 401 on it — found via live smoke-testing, not by reading the code. Render (or any PaaS) needs an unauthenticated 200 to confirm the service is up, so this would have broken deployment health checks. Fixed with `@Public()`; verified `GET /` now returns 200 live.
- **Uzbekified the last English user-facing strings**: `auth.controller.ts`'s three `NotImplementedException` messages (phone/OTP register/login/OTP stubs) were in English; translated to Uzbek per CLAUDE.md's Uzbek-only text rule.
- **Migration `0006_enable_rls.sql`**: enables row-level security (no policies = deny-all) on every table, as defense-in-depth. Changes nothing functionally today — the backend always connects with `SUPABASE_SERVICE_ROLE_KEY`, which bypasses RLS regardless of whether it's enabled — but closes the door on an *accidental future* mistake (e.g. if an anon key were ever exposed to the frontend, which it currently never is).
- **`render.yaml`** (repo root): a Render Blueprint for the backend service (`rootDir: backend`, build/start commands, health check path `/`, every required env var listed with `sync: false` so Render prompts for the real secret values in its dashboard rather than storing them in the repo; `JWT_SECRET` uses `generateValue: true` so Render mints a strong random one automatically).
- Backend `npm run build`, `npm run lint`, and `npm test` all clean. Confirmed by running `node dist/main.js` (compiled build, not `nest start --watch`) against dummy env vars — the closest thing to a live smoke test possible without a real Supabase project.
- **Minor observation, not fixed**: `DATABASE_URL` is documented in CLAUDE.md §33 and `.env.example` but is never actually read anywhere in `backend/src` — the app only uses `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` via the `supabase-js` client. Left `.env.example` as-is (matches the spec doc) but didn't add it to `render.yaml` since setting it would do nothing.

### Done together with the user — Supabase production project (step 1 of the checklist below)

- Project created at supabase.com, ref `whjhdmxzehonhootuabp`. Migrations applied via the Supabase CLI run through `npx supabase` (no global install needed — `npx -y supabase --version` just works): `supabase login` (browser OAuth — had to be run in the user's own real Terminal, since this sandboxed shell has no TTY/browser and can't do the OAuth flow), `supabase link --project-ref whjhdmxzehonhootuabp`, `supabase db push`.
- **Caught a real mistake mid-setup**: the user's first `link`/`db push` attempt was run from their home directory (`~`) instead of the repo root, so the CLI found zero local migrations and reported "Remote database is up to date" — which sounded like success but actually meant nothing had been pushed. Caught by noticing the prompt showed `~` instead of the project path, confirmed by finding a stray `~/supabase/.temp` with no `migrations/` folder. Re-ran from the correct directory and all 6 migrations applied for real.
- Verified with `npx supabase migration list` (local/remote columns match for 0001–0006) and independently by connecting directly with `@supabase/supabase-js` using the real `service_role` key (read-only `count` query on `users` → 0 rows, as expected for a fresh DB).
- `backend/.env` (gitignored, never committed) now has real `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY`. To avoid the service-role key or JWT secret ever appearing in this conversation: `SUPABASE_URL` was filled in directly (not secret — it's derived from the project ref the user already shared), `JWT_SECRET` was generated with `openssl rand -base64 48` and written straight to the file from a script without ever being printed, and the user pasted only `SUPABASE_SERVICE_ROLE_KEY` in themselves via `open -e .env` (TextEdit) after `nano` proved awkward for them.
- **Full live verification**: booted the actual compiled backend (`node dist/main.js`) against the real production Supabase. `GET /` → 200. `GET /debts/confirm/<valid-uuid>` (nonexistent) → correct `404 Havola yaroqsiz yoki muddati o'tgan`, a real database round-trip. Bonus finding: hitting that same route with a non-UUID-shaped token surfaces a raw Postgres `invalid input syntax for type uuid` error internally — and confirmed live that the Phase 10 exception filter correctly masks it to the generic Uzbek message rather than leaking it, exactly as designed.

### What's still needed — and why it wasn't done autonomously

Everything below needs a real Render/hosting account and real secrets that only the project owner has. These are irreversible-ish, credentialed, external-service actions (creating accounts, spending on paid tiers if free tier is exceeded, deploying something publicly reachable) — not something to do without the user directly in the loop. This is the manual checklist for whoever runs deployment:

1. ~~**Supabase production project**~~ — done, see above.
2. ~~**Telegram bot**~~ — done: created via @BotFather, `@QarzDaftar_uzBot` (first_name "qarzdaftar", id `8984296001`). Token verified live via `GET https://api.telegram.org/bot<token>/getMe` (without ever printing the token in this conversation — same pattern as the Supabase key). `TELEGRAM_BOT_TOKEN` and `TELEGRAM_BOT_USERNAME` set in `backend/.env` (username normalized to omit the leading `@`). Mini App URL registered with BotFather (`/mybots` → Bot Settings → Menu Button) pointing at `https://qarzdaftar-tau.vercel.app`, once the frontend was live (step 7).
3. ~~**VAPID keys**~~ — done: generated via the `web-push` library already installed in `backend/node_modules` (no `npx` fetch needed), written directly into `backend/.env` (`VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY` — private key never printed anywhere) and `frontend/.env` (`NEXT_PUBLIC_VAPID_PUBLIC_KEY`, same public key value, confirmed to match). `VAPID_SUBJECT=mailto:komronfrx@gmail.com` (the project owner's email — the `mailto:` scheme is the standard VAPID subject when there's no deployed domain yet; can be switched to `https://<production-domain>` once the frontend has a real URL, step 7). Verified live: booted the compiled backend and confirmed `PushService`'s "VAPID keys not configured" warning (seen during earlier Phase 10 smoke tests) no longer fires.
4. ~~**`AI_API_KEY`**~~ — done: key generated at aistudio.google.com by the user, set in `backend/.env` (never shown in this conversation). **Found and fixed a real, previously-undiscovered production bug while verifying it** — Phase 7's AI reminder feature had never been tested against a real key before now:
   - `GEMINI_MODEL = 'gemini-2.5-flash'` (the model `ai.service.ts` was hardcoded to) is no longer available to new API keys at all — confirmed live via a direct API call, which returned `"This model ... is no longer available to new users."` Switched to `gemini-flash-latest`, Google's maintained alias for their current default flash model (chosen over hardcoding another dated model name, so this doesn't silently break again next time Google retires one).
   - That model runs an internal "thinking" step before producing visible text — confirmed live it burns anywhere from ~150 to ~700+ tokens on reasoning alone depending on prompt complexity. The old `maxOutputTokens: 200` budget left little to nothing for the actual reminder text (reproduced empty and garbled responses live). Raised to `1024` (verified live: comfortably covers thinking + a real Uzbek reminder message).
   - The model also doesn't reliably honor "no markdown / no quotes" as a plain instruction alone (reproduced live: got `**bold**` markers and wrapping quote marks despite the prompt explicitly forbidding both). Reinforced the prompt wording and added a `sanitizeAiText()` post-processing step (strips markdown `**` and wrapping quote characters) as a defensive backstop rather than relying on prompt compliance alone.
   - All three fixes verified together by calling the actual compiled `AiService.generateReminder()` (not just raw `curl` — the real production code path, prompt-building included) with a fake debt object: returned a clean, well-formatted, properly-toned Uzbek reminder with no markdown or stray quotes. Also confirmed the best-effort `ai_reminder_logs` insert correctly no-ops (FK violation on the fake user id, silently swallowed as designed) rather than polluting the production table with test data.
   - `backend/src/ai/ai.service.ts` changed; `npm run build` + `npm run lint` clean.
5. ~~**`JWT_SECRET`**~~ — done (generated with `openssl rand -base64 48`, written straight to `backend/.env`, never printed). Render's blueprint will auto-generate a separate one for the deployed instance (`generateValue: true`) — the local dev one won't be reused there.
6. ~~**Deploy the backend to Render**~~ — done: repo pushed to `github.com/komronxamdamov088-debug/qarzdaftar` (first-ever commit for this project — reviewed everything staged before committing; caught and excluded `supabase/.temp/` — the Supabase CLI's local link-state cache, which had leaked into the working tree and contained the project's org id/pooler hostname — and `.claude/settings.local.json`, neither of which belong in the repo; no `.env` files were ever staged, only `.env.example`). Render Blueprint deploy from that repo **failed on the first attempt**: `Exited with status 127 while building your code`. Diagnosed and fixed: `render.yaml` set `NODE_ENV=production` as an env var, and npm's legacy behavior is to skip `devDependencies` on `npm install` whenever `NODE_ENV=production` is set — which silently dropped `@nestjs/cli` (the package providing the `nest` binary that `npm run build` shells out to), hence `nest: command not found` → exit 127. Fixed `buildCommand` to `npm ci --include=dev && npm run build`, which installs dev dependencies regardless of `NODE_ENV`; reproduced the failure and confirmed the fix locally (`NODE_ENV=production npm ci --include=dev` → `node_modules/.bin/nest` exists → build succeeds) before pushing. Redeployed successfully. **Live at `https://qarzdaftar-backend-kx8c.onrender.com`** — verified via `curl`: `GET /` → 200 with helmet/rate-limit headers present, `GET /debts/confirm/<uuid>` → correct `404 Havola yaroqsiz` (a real round-trip to production Supabase, proving the Render-side env vars are correctly set), `GET /users/me` → safe `401`.
7. ~~**Deploy the frontend**~~ — done: deployed to Vercel from the same `qarzdaftar` GitHub repo. **Hit two real snags along the way**:
   - Vercel's newer monorepo-onboarding UI defaulted to an "Application Preset: Services" mode that auto-detected *both* `frontend` (Next.js) and `backend` (NestJS) as services to deploy together in one Vercel project — not what we wanted, since the backend is already on Render and isn't built to run as a Vercel Function (it has a persistent `@nestjs/schedule` cron job for the reminder scheduler, which doesn't fit a serverless request/response model). Guided the user to back out of "Services" mode; Vercel then correctly showed a single-app "Next.js" preset with Root Directory already set to `frontend`.
   - Guessed the assigned domain would be `qarzdaftar.vercel.app` (matching the project name) and had the user set `NEXT_PUBLIC_APP_URL` to that — **wrong**: that domain belongs to an unrelated third party's old Create React App project (confirmed live via `curl` — got back `<title>React App</title>`, not QarzDaftar). Vercel had actually assigned `qarzdaftar-tau.vercel.app` (the `-tau` suffix because the exact name was taken). Caught by curling the guessed domain and noticing the response didn't match, corrected `NEXT_PUBLIC_APP_URL` to the real domain, redeployed. This mattered beyond cosmetics — that env var is baked into the debt-confirmation share link (`debts/[id]/page.tsx`), so the wrong value would have produced broken confirmation links.
   - **Live at `https://qarzdaftar-tau.vercel.app`** — verified via `curl`: `<title>QarzDaftar</title>` and the correct Uzbek meta description, `manifest.webmanifest` returns the correct PWA manifest (name, theme color, start_url), `/dashboard` returns 200 (renders the sign-in-required state without crashing, as expected with no session cookie).
8. ~~**Close the loop on CORS**~~ — done: `CORS_ORIGINS=https://qarzdaftar-tau.vercel.app` set on the Render backend (had to be added as a brand-new variable — it didn't exist yet, since it had been left blank during the original Blueprint apply and Render doesn't create an entry for a blank `sync: false` value). Verified live via `curl` with an `Origin` header: the Vercel origin gets `access-control-allow-origin` back, an arbitrary origin (`evil.example.com`) does not.
9. **Live testing** (now unblocked — 1–8 are done): the full CLAUDE.md §45 checklist — click through auth (Telegram login), debts CRUD, payments, confirmation link, push notification permission + a real push, Telegram bot notification delivery, AI reminder generation, admin bootstrap (`update users set role='admin'` in Supabase, per Phase 9's bootstrap note) and the full admin panel, PWA install prompt, responsive breakpoints on a real device, and the Mini App inside an actual Telegram client. Not yet done — everything so far has been verified via `curl`/API calls, not by clicking through the actual UI in a browser or Telegram client.
10. **Monitoring**: Render has basic built-in logs/metrics on the free tier; Vercel has the same. Nothing beyond that has been set up (no Sentry/error-tracking service — would need its own account + DSN, another user-driven step).

**Not tested live in a browser/Telegram client (item 9)**: everything else (1–8) is genuinely live in production now and was verified via real HTTP requests (`curl`) against the real deployed services — this is no longer a local-only smoke test. What's left is clicking through the actual UI as a real user would, which needs a browser/Telegram session, not just API calls.

---

## Phase 11 — Payment links, Click/Payme/Yagona Pay, Bilingual UI (UZ/RU), Receipts, Telegram deep-linking ✅ CODE-COMPLETE (manual/live testing with real provider credentials still outstanding)

New feature set requested by the user on top of the already-shipped, live-in-production Phases 1–10 above. Full approved plan (context, rationale, every stage in detail) is saved at `/Users/komron/.claude/plans/binary-plotting-elephant.md` — **read that file first when resuming**, this section is the status tracker, that file is the design doc. Do not re-plan or re-ask the scope questions already answered below.

### Locked-in decisions (already approved by the user, do not re-litigate)

- Build the **full** Click/Payme/Yagona Pay provider architecture (checkout URL, webhook, signature verification, idempotency) with **no real credentials yet** — every provider call must throw a clear "not configured" error (mirroring how `AiService` behaves when `AI_API_KEY` is unset). **Never fake a successful payment.**
- The debt payment link **extends the existing** `/confirm/[token]` confirmation link (same `debts.confirmation_token`) rather than a new separate link/token system.
- i18n (Uzbek + Russian) is being done as **one full phase first**, translating the entire existing UI, *before* building payment/receipt/deep-link features — so those are bilingual from day one.
- Receipts are generated for **every** successful payment (cash and provider), not just provider ones — per the user's own spec wording ("har bir muvaffaqiyatli to'lovdan keyin").
- Provider checkout is for the **full remaining amount only** in v1 (no partial-amount checkout yet).
- Yagona Pay's exact webhook field/signature names are **provisional** (less-documented aggregator) — built against the same interface as Click/Payme, flagged for verification once real docs/credentials exist.

### Stage 0 — Fix raw-body gap ✅ DONE

- `backend/src/main.ts`: `NestFactory.create(AppModule)` → `NestFactory.create(AppModule, { rawBody: true })`. Needed later so Click/Payme webhook signature verification can hash the exact original request bytes. Backend `npm run build`/`lint` verified clean at the time.

### Stage 1 — i18n framework + full UZ/RU translation ✅ DONE (this session)

**Infra built (all new files, done):**
- `frontend/src/i18n/locale.ts` — `Locale = "uz"|"ru"`, `DEFAULT_LOCALE`, `LOCALE_COOKIE = "qd_locale"`, `isLocale()`.
- `frontend/src/i18n/dictionaries/uz.ts` — the canonical dictionary (source of truth for the `Dictionary` type, exported as `export type Dictionary = typeof uzDictionary`). Nested by feature area (`common`, `nav`, `errorState`, `signInRequired`, `home`, `dashboard`, `debtsPage`, `debtDetail`, `addPayment`, `paymentList`, `aiReminder`, `copyLink`, `deleteDebt`, `reminderPicker`, `editDebt`, `newDebt`, `activity`, `profile`, `notificationSettings`, `statistics`, `statsProgressBar`, `confirmPage`, `confirmActions`, `admin`, `debtStatus`, `confirmationStatus`, `reminderStatus`, `language`, `apiErrors`). Parameterized strings (e.g. dashboard greeting, confirmation-link description) are typed functions, not template placeholders, e.g. `dashboard.greeting: (name: string) => string`.
- `frontend/src/i18n/dictionaries/ru.ts` — full Russian translation, typed as `const ruDictionary: Dictionary = {...}` so **a missing/extra key is a TypeScript compile error** (this has not been verified by an actual `npm run build` yet this session — do that first when resuming).
- `frontend/src/i18n/dictionaries.ts` — `getDictionary(locale)` registry.
- `frontend/src/i18n/get-locale.ts` — server-only `getLocale()`, reads the `qd_locale` cookie only (no extra API round-trip), defaults to `"uz"`.
- `frontend/src/i18n/locale-context.tsx` — `"use client"` `LocaleProvider` + `useTranslations()` hook (returns `{locale, dict}`) for client components.
- `frontend/src/i18n/locale-actions.ts` — `setLocaleAction(locale)` server action: sets the cookie immediately (works for anonymous/public visitors too), and best-effort PATCHes `/users/me/locale` when signed in. **Not yet wired to any UI** — the language-switcher component itself doesn't exist yet (see "Next step" below).

**Backend locale plumbing (done):**
- Migration `supabase/migrations/0007_users_locale.sql` — `users.locale text not null default 'uz' check (locale in ('uz','ru'))`. **Not yet applied to the live Supabase project** — needs `supabase db push` same as prior migrations (see Phase 10 for the exact command/directory gotcha).
- `backend/src/database/database.types.ts` — added `UserLocale` type + `locale` column on the `users` Row/Insert types.
- `backend/src/users/dto/update-locale.dto.ts` (new), `UsersService.updateLocale()`, `UsersController` `PATCH /users/me/locale` — all done, backend build verified clean.
- `backend/src/auth/auth.service.ts` — `AuthResult.user` now includes `locale` so `frontend/src/lib/telegram-auth.ts` can seed the `qd_locale` cookie from the account's saved locale on first login (only if no cookie already exists on that device — a pre-login switch always wins). This part of `telegram-auth.ts` is done; its own error-fallback string ("Telegram orqali kirishda xatolik yuz berdi.") is **not yet** converted to use the dictionary — small leftover.
- Backend `code` fields for user-facing exceptions: **only `backend/src/payments/payments.service.ts`'s `mapRpcError()` done** (each case now returns `{code, message}` instead of a bare string — `DEBT_NOT_FOUND`, `FORBIDDEN`, `DEBT_NOT_PAYABLE`, `INVALID_AMOUNT`, `AMOUNT_EXCEEDS_REMAINING`). **`debts.service.ts` and `reminders` module's user-facing exceptions still need the same `{code, message}` treatment** — not started.
- `frontend/src/lib/api.ts` — `extractApiErrorMessage(error, fallback, codeMessages?)` now accepts an optional code→message map and prefers it when the backend response includes a matching `code`. Frontend `dict.apiErrors` is passed as that map from converted server actions.
- Backend notification-only i18n (`backend/src/common/i18n/notifications-i18n.ts`, for push/Telegram text that never crosses an HTTP response body) — **not started**, needed before Stage 3's payment notifications and to update `reminder-scheduler.service.ts`.

**`lib/format.ts`** — done: `formatSom`, `formatDate`, `formatNumber` (new), `statusLabel`, `confirmationStatusLabel`, `reminderStatusLabel` all take an optional `locale` param (default `"uz"`, so any not-yet-updated call site still compiles and still shows Uzbek — safe incremental migration). Label maps are now `Record<Locale, Record<string,string>>`.

**Frontend files converted to the dictionary (done, prior session):**
`app/layout.tsx` (locale-aware `<html lang>` + `LocaleProvider` wiring), `app/page.tsx`, `components/bottom-nav.tsx` (also picked up a small bottom safe-area padding class, harmless early piece of Stage 5), `components/error-state.tsx` (now an async Server Component that reads locale itself), `components/sign-in-required.tsx` (same), `components/summary-card.tsx` (+`locale` prop), `components/debt-card.tsx` (+`locale` prop), `components/debt-filters.tsx`, `app/(app)/dashboard/page.tsx`, `app/(app)/debts/page.tsx`, `app/(app)/debts/[id]/page.tsx`, `app/(app)/debts/[id]/actions.ts`, `app/(app)/debts/[id]/add-payment-form.tsx`, `app/(app)/debts/[id]/ai-reminder-picker.tsx`, `app/(app)/debts/[id]/copy-link-button.tsx`, `app/(app)/debts/[id]/delete-debt-button.tsx`, `app/(app)/debts/[id]/payment-list.tsx` (+`locale` prop), `app/(app)/debts/[id]/reminder-picker.tsx`, `app/(app)/debts/[id]/edit/edit-debt-form.tsx`, `app/(app)/debts/[id]/edit/page.tsx`, `app/(app)/debts/new/actions.ts`, `app/(app)/debts/new/add-debt-wizard.tsx`.

**Verified this session:** `npm run build` + `npm run lint` both clean in `frontend/` for the prior session's ~25-file batch above — this was the very first step taken this session (per the "Immediately next step" note left last time) and confirmed nothing was broken before converting more files.

**Frontend files converted to the dictionary (done, this session, item 1 and item 2 of the ordered list below):**
- `app/(app)/activity/page.tsx` — now reads `locale`/`dict` via `getLocale()`/`getDictionary()`, uses `dict.activity.title`/`dict.activity.empty`, passes `locale` down to `NotificationItem`.
- `app/(app)/activity/notification-item.tsx` — added optional `locale?: Locale` prop (default `DEFAULT_LOCALE`, same pattern as `PaymentList`), passed into `formatDate(notification.created_at, locale)`. (`notification.title`/`.body` themselves are backend-generated dynamic strings, not static UI copy, so left as-is — no dict key exists or is needed for those.)
- `app/(app)/activity/actions.ts` — `markNotificationReadAction` converted to the established `getDictionary(await getLocale())` + `dict.apiErrors.AUTH_REQUIRED`/`dict.apiErrors.GENERIC` + `extractApiErrorMessage(error, dict.apiErrors.GENERIC, dict.apiErrors)` pattern (exact mirror of `debts/[id]/actions.ts`).
- `app/(app)/profile/page.tsx` — converted `Statistika`/`Admin panel` links to `dict.profile.statistics`/`dict.profile.adminPanel`; mounted new `LanguageSwitcher` component in its own card section between the admin-panel link and `NotificationSettings`.
- `app/(app)/profile/language-switcher.tsx` — **new file**, the previously-missing language-switcher UI: client component, `useTranslations()` for `dict.language.label`/`dict.language.uz`/`dict.language.ru`, two pill buttons in a rounded track (active = `bg-primary text-white`), calls `setLocaleAction(next)` from `i18n/locale-actions.ts` inside a `useTransition`, no-ops if already on that locale or a transition is pending.
- `app/(app)/profile/notification-settings.tsx` — every hardcoded string (title, device-push label, enable/disable button text, unsupported message, both toggle labels, all 5 error messages: permissionDenied/notConfigured/incompleteSubscription/enableError/disableError) replaced with `dict.notificationSettings.*` via `useTranslations()`.
- `app/(app)/profile/actions.ts` — all three actions (`subscribeToPushAction`, `unsubscribeFromPushAction`, `updateNotificationPreferencesAction`) converted to the same `getDictionary(await getLocale())` + `dict.apiErrors.*` pattern as `activity/actions.ts`.

**Verified this session:** `npm run build` + `npm run lint` both clean in `frontend/` after the activity + profile conversions above (including the new `language-switcher.tsx` file); clean again after the statistics conversion; clean again after the confirm-page conversion; clean again after the six admin files; clean again after the `telegram-auth.ts` leftover fix (each batch was verified independently, not just once at the end).

**Frontend files NOT yet converted (still hardcoded Uzbek-only strings) — do these next, in this order:**
1. ~~`app/(app)/activity/page.tsx`, `notification-item.tsx`, `actions.ts`~~ — done, see above.
2. ~~`app/(app)/profile/page.tsx`, `notification-settings.tsx`, `actions.ts`, language-switcher~~ — done, see above.
3. ~~`app/(app)/statistics/page.tsx`, `components/stats-progress-bar.tsx`~~ — done, this session: `statistics/page.tsx` now reads `locale`/`dict` via `getLocale()`/`getDictionary()`, all 5 `SummaryCard` labels + empty-state text + title use `dict.statistics.*`, `locale` passed to every `SummaryCard` and to `StatsProgressBar`. `StatsProgressBar` gained an optional `locale?: Locale` prop (default `DEFAULT_LOCALE`, same pattern as `PaymentList`), uses `dict.statsProgressBar.*` for its label/legend text and `formatSom(amount, locale)`. `npm run build` + `npm run lint` both clean in `frontend/` after this change.
4. ~~`app/confirm/[token]/page.tsx`, `confirm-actions.tsx`~~ — done, this session: `page.tsx` (public, unauthenticated — reads locale via `getLocale()`'s cookie fallback, no `getServerToken()` gate) now uses `dict.confirmPage.linkInvalid`/`.header(lender, borrower, amount)`/`.dueDate(date)`/`.disclaimer`, `formatSom`/`formatDate` both passed `locale`. `confirm-actions.tsx` (client component) uses `useTranslations()` from `i18n/locale-context` for `dict.confirmActions.*` (confirmed/rejected/confirm/reject labels) and passes `dict.apiErrors` as the `codeMessages` map to `extractApiErrorMessage`, mirroring the pattern already used in server actions. `npm run build` + `npm run lint` both clean in `frontend/` after this change.
5. ~~`app/admin/layout.tsx`, `admin/page.tsx`, `admin/reports/page.tsx`, `admin/users/page.tsx`, `admin/users/actions.ts`, `admin/users/role-toggle-button.tsx`, `components/admin-stat-tile.tsx`~~ — done, this session: all six files now use `dict.admin.*` (nav labels, brand, access-denied copy, dashboard tile labels via `dict.admin.tiles.*`, reports titles/table headers, user-role/telegram-connected labels, role toggle button promote/demote text, `roleUpdateError`). `admin-stat-tile.tsx` gained an optional `locale?: Locale` prop and now calls `formatNumber(value, locale)` instead of hardcoded `.toLocaleString("uz-UZ")`; same swap applied to the report tables' row counts in `admin/reports/page.tsx`. `admin/users/actions.ts` converted to the standard `getDictionary(await getLocale())` + `dict.apiErrors.AUTH_REQUIRED`/`dict.admin.roleUpdateError` pattern; `role-toggle-button.tsx` (client component) uses `useTranslations()`.
6. ~~Leftover: `lib/telegram-auth.ts`'s own fallback error string~~ — done: added a new `home.telegramLoginError` key to both `uz.ts` and `ru.ts` dictionaries (this string previously had no dict key anywhere), `telegram-auth.ts`'s catch block now does `getDictionary(await getLocale())` and passes `dict.home.telegramLoginError`/`dict.apiErrors` to `extractApiErrorMessage`.
7. ~~Backend: `{code, message}` for `debts.service.ts`/`reminders` module + `notifications-i18n.ts`~~ — done, this session:
   - `backend/src/debts/debts.service.ts`: every deliberately-authored user-facing exception now carries a `code` alongside its Uzbek `message` (mirroring `payments.service.ts`'s existing `mapRpcError()` pattern) — `CANNOT_DEBT_SELF` (self-debt guard in `create()`), `DEBT_NOT_FOUND` (reused, matches the code `payments.service.ts` already uses), `FORBIDDEN` (reused, `assertOwnership()`), `INVALID_TOKEN` (`findByToken()`), `DEBT_ALREADY_REVIEWED` (both places in `confirmByToken()` that throw it). `findById()`'s bare `NotFoundException('Qarz topilmadi')` was deliberately left untouched — its own doc comment says it's for internal server-side use only (the reminder scheduler), never exposed via a controller, so no frontend ever parses its body.
   - `backend/src/reminders/reminders.service.ts`: `NO_DUE_DATE` (missing due date in `create()`), `REMINDER_ALREADY_SCHEDULED` (unique-violation case in `mapInsertError()`).
   - New `backend/src/common/i18n/notifications-i18n.ts`: a small server-side-only UZ/RU dictionary (`getNotificationsI18n(locale)` → `{reminderTitle, reminderBody(name, amount)}`) plus a `formatSom(amount, locale)` helper (uz → `so'm`, ru → `сум`, mirrors the frontend's `lib/format.ts` currency formatting exactly) — needed because push/Telegram notification text never crosses an HTTP response body, so it can't reuse the frontend's `dict.apiErrors` code-lookup trick; the correct locale has to be resolved server-side from the recipient's `users.locale` column.
   - `backend/src/reminders/reminder-scheduler.service.ts`: replaced its own local hardcoded-Uzbek `formatSom()` + literal `'Qarz eslatmasi'` title/body with `getNotificationsI18n(user.locale)` + the shared `formatSom` from the new i18n file — a reminder recipient with `users.locale = 'ru'` now gets their push/Telegram reminder text in Russian.
   - Frontend: added the 5 new backend codes (`CANNOT_DEBT_SELF`, `INVALID_TOKEN`, `DEBT_ALREADY_REVIEWED`, `NO_DUE_DATE`, `REMINDER_ALREADY_SCHEDULED`) to `dict.apiErrors` in both `uz.ts` and `ru.ts`, closing the loop so any call site already passing `dict.apiErrors` to `extractApiErrorMessage` automatically renders these in the visitor's chosen locale with no further per-call-site changes needed.
   - Backend `npm run build` + `npm run lint` + `npm test` all clean. Frontend `npm run build` + `npm run lint` clean after the two dictionary additions (also confirms `ru.ts`'s `satisfies Dictionary` still type-checks with the 5 new keys added).

All the dictionary keys needed for every item above **already existed** in both `uz.ts` and `ru.ts` except for `home.telegramLoginError` (item 6) and the 5 new `apiErrors` codes (item 7), both added fresh this session — everything else was a mechanical swap of hardcoded strings for `dict.xxx` lookups.

**Stage 1 (i18n) is now fully DONE.** Stages 2 and 3 (below) were completed in the same session immediately after. See Stage 3's "Immediately next step" note for where to resume (Stage 4, frontend).

### Stage 2 — Database migrations for payments/receipts ✅ DONE (this session)

- `supabase/migrations/0008_payment_transactions.sql` — new `payment_transactions` table (id, debt_id fk, user_id fk, `provider` check(`click|payme|yagona_pay`), `provider_transaction_id`, `amount`, `status` check(`pending|success|failed|cancelled`), `checkout_url`, `raw_webhook_payload jsonb`, `error_message`, timestamps). Partial unique index `(provider, provider_transaction_id) where provider_transaction_id is not null` for idempotency. Reuses `set_updated_at()` from 0001. RLS enabled (deny-all, matches 0006's posture).
- `supabase/migrations/0009_payments_provider_columns.sql` — adds `user_id`, `method` (check `cash|click|payme|yagona_pay`, default `'cash'`), `provider_transaction_id`, `payment_transaction_id` to `payments`; rewrites `record_payment` to accept new optional named params `p_method default 'cash'`, `p_provider_transaction_id default null`, `p_payment_transaction_id default null` — every existing ownership/status/amount check (`DEBT_NOT_FOUND`/`FORBIDDEN`/`DEBT_NOT_PAYABLE`/`INVALID_AMOUNT`/`AMOUNT_EXCEEDS_REMAINING`) preserved byte-for-byte; existing call sites keep working unmodified since the new params are optional.
- `supabase/migrations/0010_receipts.sql` — new `receipts` table, one row per payment (`unique` on `payment_id`), a point-in-time snapshot: `payer_name`, `recipient_name`, `payment_amount` (this specific payment), `debt_original_amount`, `debt_paid_amount` (cumulative to date), `debt_remaining_amount`, `method`, `debt_status`, `locale`. `receipt_number` generated from a new `receipt_number_seq` sequence, format `QD-YYYYMMDD-NNNNNN`. RLS enabled.
- `backend/src/database/database.types.ts` updated to match: new `PaymentProviderName`/`PaymentMethod`/`PaymentTransactionStatus` types, `payments` Row/Insert gained the 4 new columns, new `payment_transactions` and `receipts` table types, `record_payment` Function `Args` gained the 3 new optional params.
- **Not yet pushed to the live Supabase project** — needs `supabase db push` from the repo root (same as the still-pending `0007_users_locale.sql` from Stage 1). This session's live verification (below, Stage 3) confirmed the pre-existing `debts` table lookups still work fine against production without these migrations applied — but nothing that touches `payment_transactions`/`receipts`/the new `payments` columns can be exercised live until they're pushed. **Do this before testing the cash "To'lov qo'shish" flow or any payment-provider flow against production.**

### Stage 3 — Backend: payment-provider architecture, receipts, webhooks ✅ DONE (this session)

New module `backend/src/payment-providers/`:
- `interfaces/payment-provider.interface.ts` — `PaymentProvider` (`isConfigured`/`buildCheckoutUrl`/`verifySignature`/`parseWebhook`/`buildSuccessResponse`/`buildErrorResponse`) + `CheckoutRequest`/`ParsedWebhookEvent`/`RawWebhookRequest`.
- `providers/base-payment-provider.ts` — shared abstract base: guards every method behind `isConfigured()`, throwing `ServiceUnavailableException({code:'PROVIDER_NOT_CONFIGURED', message:'<provider> hozircha sozlanmagan'})` before any subclass logic runs (mirrors `AiService`'s precedent exactly) — subclasses only implement the `do*` methods.
- `providers/click.provider.ts` — Click Merchant API: MD5 `sign_string` verification, Prepare/Complete `action` field. `providers/payme.provider.ts` — Payme: JSON-RPC 2.0 + HTTP Basic Auth (`Paycom:<PAYME_KEY>`), amounts in tiyin. `providers/yagona-pay.provider.ts` — HMAC-SHA256 over the raw body via an `X-Yagona-Signature` header, explicitly marked `TODO(yagona-pay)` throughout since its field/signature names are provisional (less-documented aggregator, matching the plan's own caveat). **None of the three have ever been exercised against a real provider sandbox** — implemented against public docs only; verify byte-for-byte before taking real payments.
- `payment-providers.service.ts` — registry (`getProvider(name)`), normalizes kebab-case route params (`yagona-pay`) to the DB's snake_case enum (`yagona_pay`).
- `payment-transactions.service.ts` — orchestrator: `initiateCheckout()` generates the transaction id client-side (`randomUUID()`) so `buildCheckoutUrl()` can embed it and throw `PROVIDER_NOT_CONFIGURED` *before* any DB row is written; sets `amount = debt.remaining_amount` server-side always. `handleWebhook()`: `verifySignature` (reject unverified) → load transaction row → idempotency no-op if already `success` → reject on status≠success → **reject on amount mismatch** (webhook-reported amount cross-checked against the stored amount, never trusted alone) → `PaymentsService.recordProviderPayment()` (always passes `debt.borrower_id` explicitly, preserving the `FORBIDDEN` ownership check regardless of who initiated checkout) → marks the transaction `success` → notifies the lender (push/Telegram/in-app, gated on their preferences, text from `notifications-i18n.ts` keyed by their `locale`).
- `payment-checkout.controller.ts` (authenticated `POST /debts/:debtId/payments/:provider/checkout`, ownership via `debtsService.findOneForUser`) and `public-payment-checkout.controller.ts` (`@Public() POST /debts/confirm/:token/checkout/:provider`, via `findByToken` — never the one-shot `confirmByToken`) — **both live in `payment-providers/`, not `payments/`/`debts/`**, a deliberate deviation from the original plan sketch: `PaymentTransactionsService` depends on `PaymentsService` (to apply a verified webhook), so putting checkout-initiation on `PaymentsController` would have created a module import cycle (`payments` → `payment-providers` → `payments`). Functionally identical routes, just organized in the module that actually owns the orchestration logic.
- `payment-webhooks.controller.ts` — `@Public() POST /payments/webhooks/:provider`, reads `req.rawBody`/headers via `@Req() req: RawBodyRequest<Request>` (no `@Body()` DTO, so the global `ValidationPipe` never applies — these are provider-shaped payloads). `main.ts` already had `rawBody: true` from Stage 0 (done in an earlier session).
- `payment-providers.module.ts` wires it all together, importing `PaymentsModule` (for `recordProviderPayment`), `DebtsModule`, `UsersModule`, `NotificationsModule`, `PushModule`, `TelegramModule`.

`backend/src/payments/payments.module.ts` gained `exports: [PaymentsService]` (confirmed missing, now fixed) and imports the new `ReceiptsModule`. `payments.service.ts` gained `recordProviderPayment()` (mirrors `create()`'s RPC-calling/error-mapping, called only from `PaymentTransactionsService` after signature+amount verification) and both `create()`/`recordProviderPayment()` now call `receiptsService.createForPayment()` after a successful `record_payment` — satisfying "every successful payment gets a receipt" for cash too, not just provider payments.

New module `backend/src/receipts/`:
- `receipts.service.ts` — `createForPayment()` (best-effort snapshot insert, catches+logs on failure so a receipt bug never breaks the underlying payment, same precedent as `AiService`'s `ai_reminder_logs` insert), `findAllForDebt`/`findOneForUser` (authenticated, ownership via `debtsService.findOneForUser`), `findAllByToken`/`findOneByToken` (public, same trust boundary as `DebtConfirmationController` — a valid `confirmation_token` authorizes viewing that debt's receipts), `generatePdf()` via **`pdf-lib`** (new dependency, installed — pure-JS, no headless browser).
- `receipts-i18n.ts` — small UZ/RU dictionary (~13 labels + method labels + debt-status labels) for the PDF/API response, rendered entirely server-side.
- `receipts.controller.ts` (authenticated: `GET /debts/:debtId/receipts`, `GET /receipts/:id`, `GET /receipts/:id/pdf`) + `public-receipts.controller.ts` (`GET /debts/confirm/:token/receipts[/…]`, all `@Public()`).
- `receipts.module.ts` exports `ReceiptsService` so `PaymentsModule` can use it.

`backend/src/common/i18n/notifications-i18n.ts` (from Stage 1) gained `paymentReceivedTitle`/`paymentReceivedBody(payerName, amount)` in both locales.

Both `PaymentProvidersModule` and `ReceiptsModule` wired into `app.module.ts`.

**Live-verified this session** (compiled `node dist/main.js` against the real production Supabase, no new migrations pushed yet — confirmed this doesn't matter for these specific checks since they never touch the new tables):
- `POST /payments/webhooks/click` (bogus body) → `503 {"code":"PROVIDER_NOT_CONFIGURED","message":"click hozircha sozlanmagan"}`, verified BEFORE any DB write (signature-check guard fires first).
- Same for `POST /payments/webhooks/payme` → `payme hozircha sozlanmagan`, and `POST /payments/webhooks/yagona-pay` (kebab-case URL) → `yagona_pay hozircha sozlanmagan` (confirms the kebab→snake_case normalization works).
- `POST /payments/webhooks/unknown-provider` → `404 {"code":"PROVIDER_NOT_FOUND"}`.
- `POST /debts/:id/payments/click/checkout` with no auth → `401` (confirms the authenticated checkout route is correctly behind the global `JwtAuthGuard`, same as every other authenticated route).
- `POST /debts/confirm/<bogus-uuid>/checkout/payme` → `404 {"code":"INVALID_TOKEN"}` — confirms the public checkout path's `findByToken` lookup against the real `debts` table works correctly with zero new migrations applied (it never touches the new tables until after a valid debt is found and the provider is confirmed configured).
- `GET /` (health check) still `200` throughout.
- No fake success was ever produced anywhere in this test pass — every one of the 6 requests above returned either an honest error or a 401, never a 2xx.
- Backend `npm run build` + `npm run lint` + `npm test` all clean throughout (verified twice: once after the module/controller code, once after `eslint --fix` reformatted a few files — no functional changes from the auto-fix, just prettier-style formatting).

`backend/.env.example` updated with all the new env vars from the plan's list: `FRONTEND_URL`, `CLICK_SERVICE_ID`/`CLICK_MERCHANT_ID`/`CLICK_SECRET_KEY`/`CLICK_CHECKOUT_BASE_URL`, `PAYME_MERCHANT_ID`/`PAYME_KEY`/`PAYME_CHECKOUT_BASE_URL`, `YAGONA_PAY_MERCHANT_ID`/`YAGONA_PAY_SECRET_KEY`/`YAGONA_PAY_CHECKOUT_BASE_URL`/`YAGONA_PAY_API_BASE_URL` — every one currently unset, so every provider button/route is live in the code but returns `PROVIDER_NOT_CONFIGURED` until the user supplies real credentials. **None of these are set on Render yet either** — that's a manual step for whoever goes live with real payments, same pattern as Phase 10's `AI_API_KEY` rollout.

Stage 4 (frontend) was also completed later in this same session — see its own section below for the full record, including two real bugs it surfaced and fixed via live testing.

### Stage 4 — Frontend: payment buttons, payment-link page, receipts ✅ DONE (this session, minus Telegram-share — deferred to Stage 5)

- `lib/types.ts` — added `PaymentProviderName`, `PaymentMethod`, `CheckoutResult`, `Receipt`; `Payment` extended with `method`/`provider_transaction_id`.
- `lib/payments-api.ts` (new) — `initiateAuthenticatedCheckout`, `listReceiptsForDebt`, `initiatePublicCheckout`, `listPublicReceipts`, `receiptPdfUrl()`. The PDF link is **always the public route**, even on the authenticated debt-detail page: the debt owner already possesses the debt's `confirmation_token` (it's the existing share link), so reusing the public receipts route for PDF download avoids needing any new authenticated-download/proxy mechanism (no Bearer token can be embedded in a plain `<a href>` anyway).
- `debts/[id]/payment-buttons.tsx` (authenticated, via a new `initiateCheckoutAction` in `debts/[id]/actions.ts`) and `confirm/[token]/payment-buttons.tsx` (public, calls `initiatePublicCheckout` directly from the client — same pattern as `confirm-actions.tsx`) — both follow the `ai-reminder-picker.tsx` collapsed→expanded pattern; on success, `window.location.href = checkoutUrl`; on `PROVIDER_NOT_CONFIGURED` (or any other error), shows the localized message inline via `dict.apiErrors`.
- `components/receipt-list.tsx` (new, shared between both pages) — plain list of receipt/amount/date/method + a PDF download link.
- Both `debts/[id]/page.tsx` and `confirm/[token]/page.tsx` wired up: `PaymentButtons` shown only when `isPayable` (mirrors the existing cash-payment gate), `ReceiptList` shown when there are receipts.
- Dictionary: `paymentButtons` + `receipts` blocks added to both `uz.ts`/`ru.ts`; `apiErrors` gained `PROVIDER_NOT_CONFIGURED`/`PROVIDER_NOT_FOUND`/`RECEIPT_NOT_FOUND`.
- **Deferred to Stage 5** (as originally planned there): a dedicated Telegram-share action for receipts/payment links — Stage 5's `share-debt-link-button.tsx` already covers this ground, no need to duplicate it here.
- **Follow-up 1 (post-push, same session, commit `5f9275c`)**: added `components/payment-provider-icon.tsx` — small brand-colored initial-letter badges (Click blue, Payme teal, Yagona Pay orange), rendered next to each provider's label in both `payment-buttons.tsx` variants.
- **Follow-up 2 (post-push, same session, commit `019fda8`)**: user asked for "professional" icons and a proper card layout (icon + name + description + pay button, hover/active states) instead of small pill buttons, plus explicit success/cancel/error handling on the payment redirect. Delivered:
  - `payment-provider-icon.tsx` rewritten from letter badges to **original inline SVG glyphs** per provider (cursor-click shape for Click, card+checkmark for Payme, wallet for Yagona Pay) — not reproductions of the providers' actual trademarked logos (no licensed brand assets available), but distinct, recognizable, brand-toned marks at a consistent size/style.
  - New shared `components/payment-method-picker.tsx` — each provider now renders as a full card (icon, name, `dict.paymentButtons.descriptions[provider]`, a `dict.paymentButtons.pay` pill button) with hover (`border-primary/40 bg-primary/5`) and active (`scale-[0.98]`) states; both `payment-buttons.tsx` files now just supply the `onSelect` checkout logic (server action vs public API call) and render this shared component.
  - Checkout return URLs (`payment-checkout.controller.ts` / `public-payment-checkout.controller.ts`) now append `?payment=pending`; both `debts/[id]/page.tsx` and `confirm/[token]/page.tsx` read that via `searchParams` and show a `dict.paymentButtons.pendingBanner` info banner after a redirect back from a provider. **This is a UI hint only** — real payment success is still exclusively decided by a verified webhook (`PaymentTransactionsService.handleWebhook`), never by this query param, so it can't be used to spoof success.
  - `dict.paymentButtons` gained `pay` and `descriptions` (per-provider) keys, plus `pendingBanner`, in both `uz.ts`/`ru.ts`.
  - Verified: backend `build`+`lint`+`test` and frontend `build`+`lint` all clean; live `next dev` + `curl` smoke test against the local backend showed no runtime errors on `/dashboard`, `/confirm/[token]` (with and without `?payment=pending`).

**Deployment note:** this entire Phase 11 body of work (Stages 1–4, both commits) was pushed to `main` (`48e94ca`, then `5f9275c`) only after the user asked why payment buttons weren't showing in their Mini App — every prior session's Stage 1–4 work had been sitting uncommitted in the working tree the whole time, never deployed. **Lesson: code changes in this repo are not live until pushed** — always push after a chunk of work is verified, or explicitly flag to the user that changes are local-only, so this gap doesn't recur. Supabase migrations `0007`–`0010` were also applied to production this session (`supabase db push` from the repo root; a still-valid CLI login session from Phase 10 meant no fresh `supabase login` was needed this time) — `supabase migration list` confirmed local/remote now match through `0010`.

### Critical production bug found and fixed (same session, after the above): `record_payment` overload ambiguity broke every payment

After deploying and applying migrations, the user reported "server error" when trying to add a payment. Root cause, confirmed live via `supabase db query --linked` (the CLI's `db query` subcommand runs arbitrary read/write SQL directly against the linked remote project — useful for this kind of live diagnosis, not previously used in this project):

- Migration `0009`'s `create or replace function record_payment(p_debt_id uuid, p_user_id uuid, p_amount numeric, p_note text default null, p_method text default 'cash', p_provider_transaction_id text default null, p_payment_transaction_id uuid default null)` did **not** replace the original 4-parameter function from migration `0002`. Postgres identifies function overloads by their *full declared parameter type list* — appending 3 new trailing parameters (even with defaults) makes it a *different* signature, so `CREATE OR REPLACE` created a **second overload** alongside the original, which was never dropped.
- Every real call site (`PaymentsService.create()`, the manual/cash payment flow — and this affected provider payments identically) calls `.rpc('record_payment', {p_debt_id, p_user_id, p_amount, p_note})` with exactly 4 named parameters. With two overloads present, that call matched **both** (the 7-parameter one via its own defaults for the other 3), so PostgREST/Postgres couldn't resolve which one to call and raised `function record_payment(...) is not unique` — masked by the global exception filter into the generic "Serverda xatolik yuz berdi" every user saw. **This broke 100% of payments (cash and provider) in production** from the moment `0009` was pushed until this fix.
- Confirmed the exact duplicate via `select proname, pg_get_function_identity_arguments(oid) from pg_proc where proname = 'record_payment'` — two rows came back (4-arg and 7-arg) before the fix, one row (7-arg only) after.
- **Fix**: new migration `supabase/migrations/0011_fix_record_payment_overload.sql` — `drop function if exists record_payment(uuid, uuid, numeric, text);`, leaving only the 7-parameter version, which every existing call site already matches correctly (defaults fill the 3 new trailing params). Applied to production via `supabase db push`.
- **Verified live, twice**: (1) `supabase db query --linked` calling `record_payment(...)` with the original 4 named args against a nonexistent debt id now returns a clean `DEBT_NOT_FOUND` (P0001) instead of the "not unique" ambiguity error; (2) booted the compiled backend locally against production and confirmed `POST /debts/:id/payments` (no auth) still correctly returns `401` — i.e. the request pipeline up to the RPC call is intact, and the RPC itself no longer errors on ambiguity.
- **Lesson for any future Postgres function signature change**: `CREATE OR REPLACE FUNCTION` only replaces a function whose declared parameter type list is identical — adding, removing, or reordering parameters (even ones with defaults) silently creates a new overload instead of replacing the old one, unless the old one is explicitly `DROP FUNCTION`ed first (or in the same migration). Always check `pg_proc` for the function name after this kind of change, especially in migrations that widen an existing RPC's signature (as `payments`/`reminders`/similar patterns in this project may do again in the future).
- Committed and pushed as its own migration file (no backend/frontend code changes needed — the bug was purely in the SQL migration, and the application code was already correct).

### Follow-up bug (same session, after the above): receipt PDF didn't download inside the Telegram Mini App

User reported the receipt PDF link didn't work from inside the Mini App. Root cause: `components/receipt-list.tsx` used a plain `<a href={pdfUrl} target="_blank">` — Telegram's Mini App WebView doesn't reliably let a page open a new tab/download a file this way. **Fix**: new `components/receipt-pdf-link.tsx` (client component) — on click, checks `window.Telegram?.WebApp`; if present, calls `webApp.openLink(url)` (Telegram's documented API for handing a URL to the device's real browser) instead of letting the anchor navigate; falls back to normal `<a>` behavior outside the Mini App. Added the `openLink` method to `frontend/src/types/telegram.d.ts`'s `TelegramWebApp` interface (previously untyped/unused). `receipt-list.tsx` now renders `<ReceiptPdfLink>` instead of a raw `<a>`. Verified: frontend `build`+`lint` clean, live `next dev` smoke test showed no errors on the touched pages. Pushed as commit `045838b`.

**Two real bugs found and fixed via live dev-server testing this session** (not just `npm run build`/`lint`, which both stayed green throughout and caught neither):

1. **Critical, pre-existing, dated back to Stage 1 (i18n) — the entire app was broken in production/dev runtime.** `frontend/src/app/layout.tsx` passed the whole `dict` object (containing function values like `dashboard.greeting: (name) => ...`) as a prop from the root layout (a Server Component) into `<LocaleProvider dict={dict}>` (a Client Component). React Server Components cannot serialize functions across the server→client prop boundary — this throws `Error: Functions cannot be passed directly to Client Components...` on **every single page render**. `npm run build` never caught this because most routes are marked dynamic (ƒ) and only actually render at request time, not at build time — build/type-check are silent on this class of bug. It was only caught this session by actually starting `next dev` and curling pages, which is the live-verification step CLAUDE.md §43 requires for frontend changes and which apparently was skipped after the Stage 1 i18n work in an earlier session (task.md's own prior notes only ever recorded `npm run build`/`lint`, never a live server hit, for every i18n-touched page from Stage 1 onward — this went undetected for the entire multi-session Stage 1 effort). **Fix**: `LocaleProvider` (`frontend/src/i18n/locale-context.tsx`) now takes only `locale` (a plain string, serializable) as a prop and calls `getDictionary(locale)` itself internally via `useMemo` — dictionaries are pure, side-effect-free lookups, so it's safe to call client-side; the dictionary object itself never crosses the RSC boundary. `layout.tsx` simplified to match (dropped its now-unused `dict` variable). **Verified live**: home/dashboard/debts/profile/statistics/admin/activity/confirm all return 200 with no server errors in the dev log, confirmed both before (500 everywhere) and after (200 everywhere) the fix.
2. **This session's own regression, caught before it ever shipped**: both new receipt-fetching call sites (`debts/[id]/page.tsx`'s `loadDebt` and `confirm/[token]/page.tsx`'s `loadPublicDebt`) originally used `Promise.all([...mainFetches, listReceiptsForDebt(...)])` — since the `receipts` table doesn't exist in production yet (migrations `0008`–`0010` not pushed), a receipts-fetch failure would reject the whole `Promise.all` and hide the actual debt/payment data behind a generic error, even though the primary content fetched fine. **Fix**: receipts are now fetched as a separate `await listReceiptsForDebt(...).catch(() => [])` step after the main `Promise.all`, on both pages — a receipts-endpoint failure now degrades gracefully to an empty receipt list instead of breaking the page. This also means Stage 4 is already safe to deploy even before the migrations are pushed (the receipts UI will just stay empty until then, not crash).

**Standing lesson for future frontend sessions**: `npm run build` + `npm run lint` passing does **not** mean the app actually renders — this session proved that decisively. Always also do a live `next dev` + `curl`/browser check on at least the touched pages before calling frontend work done, per CLAUDE.md §43/45. Both bugs above were invisible to build/lint and only surfaced by actually hitting the running server.

**Immediately next step when resuming:** Stage 5 (Telegram deep-linking + safe-area polish) is the only stage left in the Phase 11 plan. Before or alongside it: push migrations `0007`–`0010` to production Supabase (`supabase db push` from the repo root) and do a real click-through regression test of cash payments + the new payment-provider "not configured" UI states in an actual browser session with a real logged-in user (still not done — everything this session was verified via `curl`/HTTP status codes against pages rendered for an anonymous/no-session visitor, not a full authenticated click-through).

### Stage 5 — Telegram deep-linking + safe-area polish ✅ DONE (this session)

- **Backend** (`backend/src/telegram/telegram-init-data.ts`): `verifyTelegramInitData` now also reads `start_param` from the already-HMAC-verified `initData` and returns it on `VerifiedTelegramInitData`. `backend/src/auth/auth.service.ts`: `AuthResult` gained an optional top-level `startParam`, threaded through from `loginWithTelegram`. **Design choice beyond the plan's literal wording**: the frontend routes on this backend-verified value (via the login action's return value) rather than reading `window.Telegram.WebApp.initDataUnsafe.start_param` directly — consistent with CLAUDE.md §24 ("Never trust Telegram user data directly on frontend"), since `initDataUnsafe` is Telegram's own unverified parse.
- **Frontend types** (`frontend/src/types/telegram.d.ts`): added `initDataUnsafe.start_param` (kept for completeness even though unused for routing, per above), new `TelegramSafeAreaInset` interface, `TelegramWebApp.safeAreaInset`/`.contentSafeAreaInset`, widened the `onEvent`/`offEvent` union to `"themeChanged" | "viewportChanged" | "safeAreaChanged" | "contentSafeAreaChanged"`, and added `openTelegramLink(url)` (Telegram's method for opening `t.me/...` links, distinct from the existing `openLink` used for the receipt-PDF fix).
- **Login → routing**: `frontend/src/lib/telegram-auth.ts`'s `loginWithTelegramAction` now returns `startParam` on success. `frontend/src/components/telegram-bootstrap.tsx` parses it with a `debt-<uuid>`/`pay-<uuid>` regex (UUID-validated) and routes to `/debts/<id>` or `/debts/<id>?openPayment=1`; anything else (or no start_param) falls back to `/dashboard` exactly as before. **Nothing yet generates these `t.me/<bot>?startapp=debt-<uuid>` links** (e.g. in bot reminder notifications) — per the approved plan, Stage 5 only had to surface+route, not generate; confirmed via a web search that `https://t.me/<bot_username>?startapp=<param>` is the correct format for the bot's Menu-Button-registered Mini App (no extra BotFather `/newapp` short-name registration needed), so this is ready to use whenever a future session wants to add deep-link buttons to notifications.
- **`openPayment` query param**: `frontend/src/app/(app)/debts/[id]/payment-buttons.tsx`'s `PaymentButtons` gained an optional `defaultOpen` prop (initial `useState` value); `debts/[id]/page.tsx` reads `openPayment` from `searchParams` and passes `defaultOpen={openPayment === "1"}` — so a `pay-<uuid>` deep link lands on the debt page with the payment-method picker already expanded.
- **Safe-area polish**: new `frontend/src/components/telegram-safe-area-sync.tsx` (mounted globally in `layout.tsx`, same pattern as the existing `telegram-theme-sync.tsx`) writes `--tg-safe-area-{top,bottom,left,right}` and `--tg-content-safe-area-*` CSS vars from `webApp.safeAreaInset`/`.contentSafeAreaInset`, re-syncing on `safeAreaChanged`/`contentSafeAreaChanged`. `globals.css` defines all 8 vars at `0px` in `:root` as the default (so plain `env(safe-area-inset-*)` stays authoritative outside the Mini App). `layout.tsx`'s `viewport` export gained `viewportFit: "cover"` (required for `env(safe-area-inset-*)` to resolve at all). `bottom-nav.tsx`'s bottom padding now combines all three sources: `max(0px,env(safe-area-inset-bottom),var(--tg-safe-area-bottom))`.
- **Share button**: new `frontend/src/components/share-debt-link-button.tsx` — inside the Mini App calls `webApp.openTelegramLink('https://t.me/share/url?url=...&text=...')` (Telegram's native share-to-chat dialog for an arbitrary URL — a different mechanism from the `startapp` deep link above, this is for sharing the link with someone else, not self-navigation); outside the Mini App falls back to `navigator.share()`, then finally to the same clipboard-copy pattern as `copy-link-button.tsx`. Mounted next to `CopyLinkButton` in the debt-detail page's confirmation-link section, sharing the same `/confirm/[token]` URL. New `dict.share.button`/`dict.share.message(name, amount)` keys added to both `uz.ts`/`ru.ts`.
- **No new env vars needed** — the share button uses Telegram's `t.me/share/url` scheme (no bot username required), so `NEXT_PUBLIC_TELEGRAM_BOT_USERNAME` was deliberately *not* added, keeping this stage self-contained.
- **Live-verified**, not just build/lint (per this session's own standing lesson above): ran `next dev` locally and curled `/`, `/dashboard`, `/debts/<bogus-uuid>`, `/debts/<bogus-uuid>?openPayment=1`, and `/confirm/<bogus-uuid>` — all `200`, zero errors in the dev server log, and confirmed the `--tg-safe-area-bottom` CSS var is actually present in the rendered `/dashboard` HTML/CSS.
- Backend `npm run build` + `npm run lint` + `npm test` clean. Frontend `npm run build` + `npm run lint` clean.
- **Not committed/pushed yet as of writing this** — see the standing lesson from earlier this session ("code changes in this repo are not live until pushed"). Confirm with the user before pushing, or push directly if operating autonomously per their standing instructions.

**Phase 11 is now fully code-complete — all 5 stages done.** What's left project-wide is the still-outstanding manual/live-testing items below (real payment-provider credentials, a full authenticated click-through in a browser/Telegram client) — nothing left to *build* per the approved plan.

### Stage 5 — Telegram deep-linking + safe-area polish — original sub-plan (kept for reference; see the as-built record directly above)

#### Payment plan — Click, Payme, Yagona Pay (Stages 2–4 ✅ ALL DONE — see those sections above for the full as-built record)

Backend and frontend both fully built and live-verified this session. One deviation from this section's original sketch: the checkout-initiation routes ended up living in `payment-providers/` rather than on `PaymentsController`/`debt-confirmation.controller.ts`, to avoid a module import cycle — see Stage 3 above for why. v1 scope is still full-remaining-amount checkout only (no partial-amount provider payments yet) — that constraint lives in `payment-transactions.service.ts`'s `initiateCheckout()`. Nothing left in this sub-plan; only Stage 5 (Telegram deep-linking) remains project-wide.

#### UZ/RU i18n plan

Covered in full detail in the Stage 1 section above — hand-rolled dictionary + React Context (not `next-intl`), `qd_locale` cookie for per-device + `users.locale` DB column for cross-device persistence, mechanical file-by-file conversion. ~60% of frontend files converted; remaining file list and next step are itemized above under Stage 1.

#### Confirmation/payment link plan ✅ DONE (Stage 3 backend + Stage 4 frontend, both this session)

- The existing `/confirm/[token]` page and `debts.confirmation_token` (uuid, unique-indexed, set at debt creation) are **reused as-is** for the payment link — no new token system.
- `POST /debts/confirm/:token/checkout/:provider` (`public-payment-checkout.controller.ts`) calls `findByToken` (never `confirmByToken`), confirmed live that it returns `INVALID_TOKEN` correctly for a bogus token.
- `app/confirm/[token]/page.tsx` now has a `payment-buttons.tsx` client component (collapsed→expanded, same pattern as `ai-reminder-picker.tsx`) and a `ReceiptList`, both confirmed rendering correctly via a live `next dev` smoke test.

#### Receipt/chek plan ✅ DONE (Stage 3 backend + Stage 4 frontend, both this session — Telegram-share explicitly deferred to Stage 5)

- Backend: `receipts` table (migration `0010`), `backend/src/receipts/` module (`createForPayment()`, `generatePdf()` via `pdf-lib`, `receipts-i18n.ts`), generated for **every** successful payment (cash and provider — `PaymentsService.create()` and `recordProviderPayment()` both call it), authenticated `receipts.controller.ts` + public `public-receipts.controller.ts`.
- Frontend: `components/receipt-list.tsx` (shared between the debt-detail page and the public payment-link page) shows receipt number/amount/date/method + a PDF download link (always via the public route — see Stage 4 above for why that's safe and correct even for the authenticated page). View is the receipt list itself (no separate single-receipt detail page built — the list already shows everything meaningful); PDF-download is a direct link; **Telegram-share is deferred to Stage 5's planned `share-debt-link-button.tsx`**, not duplicated here.

#### Telegram Mini App plan

- `backend/src/telegram/telegram-init-data.ts`: surface `start_param` from the already-HMAC-verified `initData` (currently parsed but dropped).
- `frontend/src/types/telegram.d.ts`: add `initDataUnsafe.start_param`, `safeAreaInset`, `contentSafeAreaInset` typings (all confirmed absent today), widen the `onEvent`/`offEvent` event-name union beyond just `'themeChanged'`.
- `frontend/src/components/telegram-bootstrap.tsx`: after login, read `start_param` and route (`debt-<uuid>` → `/debts/<id>`, `pay-<uuid>` → `/debts/<id>?openPayment=1`), else keep today's `/dashboard` fallback.
- New `frontend/src/components/telegram-safe-area-sync.tsx` (sibling to existing `telegram-theme-sync.tsx`): writes `--tg-safe-area-*` CSS vars from `safeAreaInset`/`contentSafeAreaInset`; combine with plain `env(safe-area-inset-*)` in `globals.css` so non-Telegram PWA installs get proper inset handling too (one small piece of this — bottom-nav padding — was already added opportunistically during Stage 1, see above). `viewportFit:"cover"` needs adding to `layout.tsx`'s `Viewport` export for `env(safe-area-inset-*)` to resolve at all.
- New `frontend/src/components/share-debt-link-button.tsx`: `Telegram.WebApp.openTelegramLink()` when inside the Mini App, else `navigator.share()`, else the existing copy-to-clipboard fallback pattern (same as `copy-link-button.tsx`).
- The Mini App already reuses the same Next.js frontend end-to-end (per CLAUDE.md §23) — nothing here is a second UI, just deep-linking + safe-area polish on top of the existing pages, which is why this is scheduled last (after payment/receipt UI already exists to link into).

### Background agents

All Explore/Plan agents spawned during this session's planning phase (2 Explore agents researching backend/frontend, 1 Plan agent designing the architecture) already completed and reported back before any code was written — none are still running. No cleanup needed.

---

## Post-Phase-11 hardening session — Yagona Pay → Yagona Pay rename, Click/Payme protocol fixes, receipt root-cause fix

User-requested audit of the full payment-provider architecture built in Phase 11 Stages 2–4, after reporting the receipt feature still didn't work. Three things were explicitly requested: (1) rename the third provider from its wrong name "Yagona Pay" to its correct name "Yagona Pay" everywhere, (2) fully verify Click/Payme/Yagona Pay's flows and fix real bugs rather than leaving them "provisional," (3) find the **root cause** of the receipt bug, not a workaround.

### 1. Yagona Pay → Yagona Pay rename ✅ DONE, migrated to production

Every reference renamed across backend, frontend, and this file — `yagona_pay`/`yagona-pay`/`YagonaPay`/"Yagona Pay" → `yagona_pay`/`yagona-pay`/`YagonaPay`/"Yagona Pay":
- `backend/src/payment-providers/providers/yagona-pay.provider.ts` → renamed to `yagona-pay.provider.ts`, class `YagonaPayProvider` → `YagonaPayProvider`.
- `payment-providers.service.ts`, `payment-providers.module.ts`, `payment-provider.interface.ts` comment, `payment-webhooks.controller.ts` comment, `database.types.ts`'s `PaymentProviderName` type, `receipts-i18n.ts`'s method labels, `backend/.env.example`'s `YAGONA_PAY_*` → `YAGONA_PAY_*` env var names.
- Frontend: `lib/types.ts`, `components/payment-method-picker.tsx`, `components/payment-provider-icon.tsx` (glyph function renamed too), both `i18n/dictionaries/{uz,ru}.ts` (`paymentButtons.providers`/`.descriptions`, `receipts.methodLabels`).
- **Database**: new migration `supabase/migrations/0012_rename_yagona_pay_to_yagona_pay.sql` — updates any existing `yagona_pay` rows (defensive no-op today, none ever existed since the provider was never configured) and rebuilds the two CHECK constraints (`payment_transactions.provider`, `payments.method`) to allow `yagona_pay` instead of `yagona_pay`. Constraint names are looked up dynamically via `pg_constraint`, not hardcoded, so the migration can't silently no-op if Postgres named them differently than expected. **Applied to production** this session (`supabase db push`, confirmed via `supabase migration list` showing local/remote match through 0012, and a direct `pg_get_constraintdef` query confirming both constraints now say `yagona_pay`).
- Two false positives deliberately left untouched: `frontend/src/app/layout.tsx` and `i18n/dictionaries/uz.ts`'s meta description both contain the ordinary Uzbek word "qulay" (= "convenient"), unrelated to the provider name — verified by reading each match before touching anything.
- Old `/payments/webhooks/yagona-pay` route now correctly resolves to `404 PROVIDER_NOT_FOUND` instead of silently misrouting — verified live. No backward-compat alias was added because no real Yagona Pay/Yagona Pay credentials or checkout link has ever existed in production to be broken by the rename (confirmed against this file's own prior history).

### 2. Click — reviewed, one real signature bug found and fixed ✅

Click's flow is two-phase (`action=0` Prepare, `action=1` Complete), distinguished by different MD5 `sign_string` formulas. The **original implementation used the same formula for both phases**, but Complete's formula inserts `merchant_prepare_id` into the string (verified via a live web search against Click's public docs and cross-referenced against the well-established community implementation pattern) — Prepare's doesn't have that field yet. Using Prepare's formula for Complete meant **every real Complete webhook would have failed signature verification** the first time real credentials were ever configured, silently breaking 100% of Click payments before they could ever succeed. There was a second, more fundamental bug: Click's Prepare webhook is only an acknowledgment ("can you accept this payment?"), but the original code treated *any* webhook where `action !== 1` as a **payment failure** — meaning Click's Prepare would always be rejected and Click would never even send the follow-up Complete call. Both fixed in `backend/src/payment-providers/providers/click.provider.ts`:
- `doVerifySignature` now branches by `action`, including `merchant_prepare_id` in the hash only for Complete.
- `doParseWebhook` maps Prepare → the new generic `phase: 'create'` (acknowledge only, never moves money) and Complete → `phase: 'complete'` (the money-moving step) — see the interface change below.
- `doBuildSuccessResponse` now returns `merchant_prepare_id` (reusing our own transaction id — Click just echoes back whatever we return here on the follow-up Complete call, so no extra stored state is needed).

### 3. Payme — reviewed, protocol was fundamentally incomplete; rewritten and fixed ✅

The original implementation only ever handled `PerformTransaction`, treating every other JSON-RPC method (`CheckPerformTransaction`, `CreateTransaction`, `CancelTransaction`) as a payment failure — exactly the same class of bug as Click's Prepare issue, but worse: **Payme always calls `CheckPerformTransaction` first**, so the original code would have rejected every real Payme payment attempt at the very first step, before `PerformTransaction` could ever be reached. Verified Payme's real method/param/response shapes via `developer.help.paycom.uz` (live doc fetches during this session, not assumed from memory) before rewriting `backend/src/payment-providers/providers/payme.provider.ts`:
- Implemented `CheckPerformTransaction` (read-only `{result:{allow:true}}` check), `CreateTransaction` (acknowledges Payme's own transaction id against our pending row, doesn't move money), `PerformTransaction` (unchanged — the actual money-moving step), and `CancelTransaction` (marks our row cancelled; explicitly **rejects** cancelling an already-completed transaction rather than silently "succeeding" at a refund that isn't implemented).
- Fixed a second real bug: the JSON-RPC 2.0 envelope's `id` field (which Payme's spec-compliant client requires echoed back verbatim in every response) was previously either missing entirely or, in the error path, had our own internal error *code string* stuffed into it by mistake (`id: code` — never valid for any interpretation of JSON-RPC). Now correctly echoed from the incoming request via a new `ParsedWebhookEvent.rpcId` field.
- Fixed a third bug: `CreateTransaction`'s response's `transaction` field was returning Payme's own transaction id; per Payme's own documented `CheckTransaction` example, this field must be **our** merchant-side reference. Fixed.
- Fixed a fourth bug (`PaymentTransactionsService`, not Payme-specific): `PerformTransaction`'s params only ever carry Payme's own `id` (no amount, no account) — the shared terminal-phase amount-mismatch check unconditionally compared `event.amount` (0, since Perform sends no amount) against the stored amount, which would have rejected **every real `PerformTransaction`** with a false `AMOUNT_MISMATCH`. Found live during testing below, fixed by skipping that check when the webhook call carried no amount at all (the amount was already verified earlier, during Check/Create).
- Perform/Cancel/CheckTransaction calls only carry Payme's own transaction id, never ours — `PaymentTransactionsService.handleWebhook` now falls back to looking our row up by `provider_transaction_id` when the gateway's payload doesn't include our own id, then backfills `event.transactionId` from the found row so every response built afterward is correct regardless of which fields that particular call happened to carry.
- Deliberately **not implemented**: `CheckTransaction` and `GetStatement` — both are read-only reporting/reconciliation methods Payme may call independently of the money-moving flow; not required for a payment to complete, left as a documented gap rather than guessed at with no sandbox to verify against.

**A fifth, more serious bug was found only through live simulation (see Testing below), not code review**: the terminal "apply payment" branch's only idempotency guard was `if (transaction.status === 'success') return success` — it had **no check for `'cancelled'` or `'failed'`**. A `CancelTransaction` followed by a late/duplicated `PerformTransaction` call for the same transaction fell straight through the missing guard and **applied the payment anyway**, despite the transaction already being cancelled. Fixed in `PaymentTransactionsService.handleWebhook`: any transaction not in `'pending'` or `'success'` state now rejects the terminal call outright, and this exact attack sequence (cancel → late perform) was re-run after the fix to confirm no payment leaks through.

### 4. Yagona Pay — architecture confirmed consistent, still fully unconfigured ✅

Single-phase webhook model unchanged (renamed only, see §1) — its field names/signature scheme remain explicitly flagged `TODO(yagona-pay)` as provisional (less-documented aggregator), same as before the rename. Frontend `payment-method-picker.tsx`'s provider list and backend `payment-providers.service.ts`'s registry both use the identical `yagona_pay` identifier — confirmed no mismatch. `isConfigured()` still correctly gates every method behind `PROVIDER_NOT_CONFIGURED` until real `YAGONA_PAY_*` env vars are set (verified live, see Testing below).

### 5. Receipt PDF — root cause found and fixed ✅ (the user's top-priority item)

**Root cause**: `receipts.controller.ts`/`public-receipts.controller.ts`'s `downloadPdf` handlers returned a raw `Buffer` from the controller method. NestJS's Express adapter (`RouterResponseController` → `ExpressAdapter.reply()`) calls `response.json(body)` for **any** object return value that isn't a `StreamableFile` — and a `Buffer` *is* a JS object. So every "PDF download," in every environment, in every session since Phase 11 Stage 3 first built this feature, actually served `{"type":"Buffer","data":[37,80,68,70,...]}` (the JSON-serialized byte array) with a `Content-Type: application/pdf` header lying about what the body actually was — never real PDF bytes. This is why the earlier session's `WebApp.openLink()` fix (commit `045838b`) didn't actually fix anything: it corrected *how* the Mini App navigates to the URL, but the URL itself never returned a valid file, in Telegram or a normal browser. **Confirmed via direct reproduction**: `Buffer.from(await PDFDocument.save())` piped through `JSON.stringify` starts with `{"type":"Buffer"` instead of `%PDF-`.
- **Fix**: both `downloadPdf` handlers now return `new StreamableFile(pdf, { type: 'application/pdf', disposition: 'attachment; filename="<receipt_number>.pdf"' })` — Nest's documented mechanism for exactly this case, which takes the `reply()` branch that pipes bytes through untouched instead of JSON-encoding them. Also fixes a second, smaller gap: `Content-Disposition` was never set before, so downloads had no real filename.
- **Verified live against a real receipt already in production** (`QD-20260817-000001`, created in an earlier session): booted the compiled backend against real Supabase, curled the public receipt PDF route, and confirmed via `file` that the downloaded bytes are a genuine "PDF document, version 1.7" — headers show `Content-Type: application/pdf`, `Content-Disposition: attachment; filename="QD-20260817-000001.pdf"`, and the correct `Content-Length`.

### Testing this session

- **Backend**: `npm run build` + `npm run lint` + `npm test` all clean after every batch of changes (Click, Payme, PaymentTransactionsService, receipts controllers).
- **Frontend**: `npm run build` + `npm run lint` clean after the rename.
- **Live end-to-end webhook simulation** (the most important verification this session): wrote a throwaway Node script that creates isolated test users/debts directly in **real production Supabase**, boots the compiled backend with **fake, self-issued test secrets** for `CLICK_SECRET_KEY`/`PAYME_KEY` (testing our own webhook receiver's correctness, not a real exchange with Click/Payme's actual servers — no real credentials involved or needed), and drives full call sequences through the real HTTP routes:
  - Click: Prepare → Complete → replayed Complete. Verified: correct signatures accepted, `merchant_prepare_id` echoed, DB stays `pending` after Prepare and only becomes `success` after Complete, exactly one `payments` row and one `receipts` row created, debt correctly marked `paid`, replay is idempotent (no duplicate payment).
  - Payme: `CheckPerformTransaction` → `CreateTransaction` → `PerformTransaction`. Verified: JSON-RPC `id` echoed on every response, `CreateTransaction`'s `transaction` field is our own id (not Payme's), DB transitions `pending` → `pending` (after Create) → `success` (after Perform), payment + receipt created.
  - Deliberate rejection cases: wrong amount at `CheckPerformTransaction` → `-31001` `AMOUNT_MISMATCH`, correctly rejected. `CancelTransaction` on a pending transaction → correctly cancelled. **`PerformTransaction` replayed against an already-cancelled transaction** — this is the run that caught bug #5 above; after the fix, correctly rejected with no payment created.
  - All test data (users/debts/transactions/payments/receipts) cleaned up after each run; the throwaway test script itself was deleted, not committed.
- **Receipt PDF**: live-verified against a real, pre-existing production receipt (see §5 above) — actual byte-level PDF validity confirmed, not just an HTTP 200.
- **Frontend page sweep**: `next dev` pointed at the local (fake-credentialed) backend, curled `/`, `/dashboard`, `/debts`, `/debts/<id>` (a real debt with a real receipt), `/statistics`, `/profile`, `/admin`, and the public `/confirm/<token>` page for that same debt — all `200`, zero errors in the dev server log. Confirmed the compiled client JS bundle contains the string "Yagona Pay" and **zero** occurrences of "Yagona Pay" anywhere, proving the rename reached what actually ships to the browser (the payment-method picker's expanded provider list is client-rendered from `useTranslations()`, so it doesn't appear in the initial server HTML — this is expected, not a bug, per the `LocaleProvider` architecture fixed earlier in Phase 11).
- **Not done, and explicitly not faked**: no real Click/Payme/Yagona Pay merchant credentials exist, so nothing here was ever tested against those providers' actual servers — only against our own webhook receiver using self-issued test secrets, which proves our code's *internal* correctness (state machine, idempotency, signature-formula self-consistency) but cannot prove byte-for-byte compatibility with each provider's real server until a real sandbox account exists for each. This is the same honest boundary the code's own comments have stated since Phase 11 Stage 3 — narrowed today from "never exercised at all" to "exercised thoroughly against a self-consistent simulation, still never exercised against the real provider."
- A real authenticated browser click-through (log in as a real user, click a payment button, get redirected to a real provider) still hasn't happened and can't happen without real provider credentials — this remains manual/future work, same as recorded in every prior Phase 11 session.

---

## Standing constraints (don't forget)

- Every backend change must pass `npm run build` + `npm run lint` (zero errors) in `backend/` before moving on.
- Every frontend change must pass `npm run build` + `npm run lint` (zero errors) in `frontend/` before moving on.
- **The app is live in production** (as of this Phase 10 session): backend on Render (`https://qarzdaftar-backend-kx8c.onrender.com`), frontend on Vercel (`https://qarzdaftar-tau.vercel.app`), a real Supabase project, a real Telegram bot (`@QarzDaftar_uzBot`), real VAPID/AI keys. Source lives at `github.com/komronxamdamov088-debug/qarzdaftar`. Every env var, key, and password was set by the user directly into `.env` files or hosting-provider dashboards — never pasted into this conversation; verification was done via `curl`/API calls that don't require seeing the secrets themselves.
- What's *not* yet done: clicking through the actual product as a user would, in a real browser and inside an actual Telegram client (Phase 10 checklist item 9). Everything verified so far is via HTTP requests, not UI interaction.
- Going forward: a code change isn't "done" until it's pushed (`git push`) — Render and Vercel both auto-deploy from the `main` branch on GitHub. Backend changes need `backend/.env`'s local values mirrored into Render's Environment tab if a *new* env var is introduced (existing ones sync automatically on redeploy).

---

## Session paused here — resume point for next session

Phase 11 Stages 0–5 plus the post-Phase-11 hardening session above are **all code-complete**. The rename + Click/Payme protocol fixes + receipt root-cause fix (this session) still need `git add`/`commit`/`push` — check `git status` on resume to see whether that already happened. Migration `0012` is already live in production (applied this session, confirmed via `supabase migration list` and a direct constraint-definition query).

**What's fully done:** all of Phase 11 (i18n, payment-provider backend architecture for Click/Payme/Yagona Pay, receipts, payment-method-picker UI, Telegram deep-linking/safe-area/share) plus this session's hardening pass (see the dedicated section above for full detail): the Yagona Pay → Yagona Pay rename (code + migrated DB constraints), Click's Prepare/Complete signature-formula bug, Payme's missing CheckPerformTransaction/CreateTransaction/CancelTransaction handling + JSON-RPC id-echo bug + amount-check bug, a serious cancel-then-late-perform payment-leak bug (found only via live simulation), and the receipt PDF's actual root cause (`Buffer` being JSON-serialized by Nest instead of streamed — fixed with `StreamableFile`).

**What's next:** nothing left to build per the approved Phase 11 plan or this session's audit. Remaining work is exclusively the manual/live-testing checklist below.

**Also still outstanding, not urgent but don't forget:**
- No real Click/Payme/Yagona Pay credentials exist yet — every provider button correctly shows "not configured" until the user supplies them (see the env var list in the "Payment plan" sub-section above, now `YAGONA_PAY_*` not `YAGONA_PAY_*`). This session's live testing used self-issued fake test secrets to verify our own webhook receiver's logic (state machine, idempotency, signature self-consistency) — it does **not** prove compatibility with any provider's real server, which needs a real sandbox account for each.
- A real authenticated click-through test (log in as a real user in a browser/Telegram client, add a cash payment, try a provider checkout, view/download a receipt) still hasn't been done — this remains the single biggest gap across every Phase 11 session's testing. Worth doing early next session: several real bugs across this project's history (the `record_payment` overload, the Telegram PDF `openLink()` fix, and now the receipt `StreamableFile` root cause) were only ever found because a *human* actually used the app — static/automated/simulated checks alone have repeatedly proven insufficient on their own.
- `DATABASE_URL` is still documented but unused (pre-existing, not this session's concern).
- Never create a separate Client Panel, never expose secrets via `NEXT_PUBLIC_*`, never fake functionality (disabled buttons with "Tez orada" instead of fake success).
- **A Supabase personal access token was pasted directly into this conversation this session** (needed to push migration `0012` since the sandbox couldn't do interactive browser OAuth) — written straight to `backend/.env` and used from there, never echoed back in any reply, but its value is present in this conversation's transcript. ✅ **Confirmed revoked by the user** (next session, 2026-08-20) — closed, no follow-up needed.

## Next session resume note (2026-08-20)

Confirmed git was already clean and fully pushed (`HEAD` == `origin/main` at `60e4318`) — the pending commit/push noted above had already happened before this check. Nothing left to build; user chose to leave the project idle rather than run a health check or start new work. When resuming: the manual/live-testing checklist in the "Session paused here" section above (real Click/Payme/Yagona Pay credentials, a real authenticated click-through in a browser/Telegram client) is still the only outstanding work.

### Follow-up (same day, 2026-08-20): stray corruption in `admin-stat-tile.tsx` found and fixed

On resuming, `git status` showed `frontend/src/components/admin-stat-tile.tsx` as modified with an uncommitted, unexplained corruption: its first line read `claudeimport { formatNumber } from "@/lib/format";` — the literal text `claude` had been prepended directly onto `import`, which is a syntax error. Origin unclear (not caused by any code change described anywhere in this file); fixed by removing the stray `claude` prefix. Verified with a full `npm run build` + `npm run lint` in `frontend/` — both clean. No functional change, no other files affected. Still nothing left to build per the rest of this file — the manual/live-testing checklist remains the only outstanding work.

---

## Phase 12 — Business (shop) accounts with admin-managed subscription gating ✅ DONE, live-verified against production (2026-08-29)

New requirement from the product owner: the Telegram Mini App is also being onboarded to shops, who use it exactly like a personal user (tracking who owes them money — the traditional "qarz daftar" ledger a shopkeeper keeps), but pay the app owner a monthly subscription **outside the app** (no in-app billing requested). The owner needs (1) a way to see which shops are using it and (2) a way to cut a shop off immediately if they stop paying. Scope was deliberately kept to the simplest version that satisfies both: no new "business" entity/table, no self-serve shop signup, no in-app billing UI — an admin manually flags/unflags an existing account.

**Data model** — migration `supabase/migrations/0013_business_accounts.sql`: `users` gained `account_type` (`personal`|`business`, default `personal`), `business_name` (nullable text), `subscription_active` (boolean, default `true`). Applied to production via `supabase db push` this session (local/remote confirmed matching through 0013 via `supabase migration list`).

**Backend**:
- `backend/src/auth/strategies/jwt.strategy.ts` — `validate()` is now `async` and re-reads `role`/`account_type`/`subscription_active` fresh from Supabase on every authenticated request (previously it trusted the JWT payload alone). If `role === 'user' && account_type === 'business' && !subscription_active`, throws `ForbiddenException({code: 'SUBSCRIPTION_INACTIVE', message: "Obuna faol emas..."})`, which blocks every single authenticated route immediately — not just on the target's next login (the JWT is valid 7 days with no revocation mechanism, so "next login" could mean days of continued access otherwise). **Admin-role accounts are always exempt from this check regardless of their own `account_type`/`subscription_active`** — a deliberate self-lockout guard so an admin can never accidentally block their own access by tagging their own account as a business and deactivating it. This also incidentally fixes a previously-documented limitation: role promotions/demotions now take effect immediately too, not just on next login, since role is no longer trusted from the stale JWT payload either.
- `backend/src/auth/auth.module.ts` now imports `DatabaseModule` so `JwtStrategy` can inject `SUPABASE_CLIENT`.
- `backend/src/admin/`: two new endpoints, both admin-only (existing class-level `@Roles('admin')` + global `RolesGuard`): `PATCH /admin/users/:id/business` (`ConvertToBusinessDto { businessName }`) sets `account_type='business'`, stores the name, and **always sets `subscription_active=true`** (the admin action itself represents "I just onboarded/activated this shop" — not a no-op rename); `PATCH /admin/users/:id/subscription` (`UpdateSubscriptionStatusDto { active }`) flips the flag, but first checks the target is already a business account and throws a `400` if not (can't "activate a subscription" on a personal account). `AdminUserSummary` gained `accountType`/`businessName`/`subscriptionActive`; `AdminService.listUsers`/`updateUserRole` select the new columns too, and a new private `toUserSummary()` helper de-duplicates the response-shaping logic that all four user-mutating methods now share.
- `backend/src/database/database.types.ts` — new `AccountType = 'personal' | 'business'`, `users` Row/Insert gained the 3 new columns.
- Backend `npm run build` + `npm run lint` + `npm test` all clean.

**Frontend**:
- `lib/types.ts` — new `AccountType`, `AdminUserSummary` gained the 3 fields. `lib/admin-api.ts` — `convertAdminUserToBusiness()`, `updateAdminUserSubscriptionStatus()`.
- `app/admin/users/business-status-control.tsx` (new) — per-row control mirroring `role-toggle-button.tsx`'s pattern: personal accounts show a "Do'konga aylantirish" button that expands to a name input + save/cancel; business accounts show the business name plus a "Faollashtirish"/"O'chirish" toggle. `app/admin/users/actions.ts` gained `convertToBusinessAction`/`updateSubscriptionStatusAction` (same `getDictionary`+`extractApiErrorMessage` pattern as the existing `updateUserRoleAction`). `app/admin/users/page.tsx` — added an "Hisob turi" column and mounted the new control alongside the existing `RoleToggleButton`.
- `components/subscription-inactive.tsx` (new) — mirrors `sign-in-required.tsx` exactly (async Server Component, own locale/dict lookup): the "obuna faol emas" screen a blocked shop sees.
- `app/(app)/layout.tsx` — the single interception point for every protected page (dashboard/debts/statistics/profile/activity all share this layout): now `async`, calls `getCurrentUser(token)` once; if that throws an `ApiError` with status 403 and `code === 'SUBSCRIPTION_INACTIVE'`, renders `<SubscriptionInactive />` instead of `children`/`BottomNav`. Chosen over touching every individual page's own fetch/catch block (which is how `SignInRequired`/`ErrorState` are currently handled per-page) specifically because this state needs to catch a blocked user on literally any page, not just ones that already call `getCurrentUser` themselves.
- New dictionary keys in both `uz.ts`/`ru.ts`: top-level `subscriptionInactive.{title,description}`; `admin.{tableAccountType,accountTypePersonal,accountTypeBusiness,convertToBusiness,businessNamePlaceholder,activateSubscription,deactivateSubscription,businessUpdateError}`; `apiErrors.SUBSCRIPTION_INACTIVE`.
- Frontend `npm run build` + `npm run lint` clean. Live `next dev` sweep (per the standing lesson from the Phase 11 session about build/lint not catching RSC runtime bugs) confirmed all 9 protected + public routes return 200 with zero errors in the dev log for an anonymous visitor (the `app/(app)/layout.tsx` change short-circuits on "no token", so the anonymous path was the main regression risk to check).

**Live end-to-end verification against production** (throwaway Node script, deleted after — same pattern as the Phase-11-hardening session's webhook simulation): booted the compiled backend against real production Supabase, created temporary test users, signed real JWTs with the real `JWT_SECRET`, and drove the full lifecycle through actual HTTP calls: personal account unaffected → admin converts to business (subscription auto-active) → still works → admin deactivates → **the exact same still-valid JWT immediately gets 403 `SUBSCRIPTION_INACTIVE` on every route tried, including `/admin/users`, with no re-login** → admin reactivates → immediately works again with the same token → attempting to toggle subscription on a personal account correctly 400s → an admin account manually tagged business + deactivated is correctly **never** blocked (self-lockout prevention confirmed live). All test rows deleted after.

**Deliberately out of scope, not built** (matches the product owner's own request, which explicitly said no in-app billing was needed yet):
- No self-serve "become a business" flow — admin-only, mirrors how the first admin account itself must be bootstrapped by hand (Phase 9's precedent).
- No in-app subscription payment — the admin toggle is a manual record of a payment that happens outside the app (bank transfer, cash, Click/Payme sent directly to the owner), not a billing integration. Revisit if/when the owner wants shops to self-pay in-app.
- No "revert business back to personal" endpoint — not requested; can be added later if needed.
- No shop-specific UX changes (faster customer search, bulk add, etc.) — the product owner mentioned wanting shop convenience but gave no concrete specifics yet; explicitly deferred until they specify what's actually needed, rather than guessing and over-building.
- "Make the app faster" — also mentioned by the product owner with no specific slow path identified; not addressed this session, deferred until a concrete complaint exists. Worth flagging: this session's own `JwtStrategy` change adds one extra indexed `users` lookup per authenticated request (previously zero-cost JWT decode) — necessary for the "cut off immediately" requirement, and not expected to be noticeable at this app's scale, but worth remembering as a real (if small) trade-off against the separate "faster" request if that resurfaces.

**Pushed to production** — commit `a833d69` on `main` (`252e3e7..a833d69`). Render (backend) and Vercel (frontend) both auto-deploy from `main`.

---

## Critical bug found immediately after the Phase 12 push: service worker permanently cached "/", silently defeating every future deploy

Right after the Phase 12 push, the user reported the Telegram Mini App and the plain website "won't open" — stuck on the static hero, no redirect to `/dashboard`, no error shown. Backend health checks (`curl` against Render, a live `/auth/telegram` call with dummy `initData`) all came back fast and correct, and a Chrome pass showed `/dashboard`/`/admin/users` correctly rendering `SignInRequired` — so the deployed code itself was healthy. First fix attempt: added visible error text to `frontend/src/components/telegram-bootstrap.tsx` (previously any login failure was swallowed silently — `if (result.ok) {...}` with no `else`, a real CLAUDE.md §41/42 "every action needs feedback" violation on its own) and pushed (`27b10f7`), expecting the user's retry to surface the real error message.

**It didn't** — after that push too, the user's screenshot still showed the bare static hero with no error text at all, meaning the newly-deployed code wasn't running on their device even after two consecutive pushes. That absence of ANY visible change across two deploys was the actual tell.

**Root cause**: `frontend/public/sw.js`'s fetch handler cached `"/"` (`SHELL_URLS = ["/"]`, the exact page `TelegramBootstrap` and the whole login flow live on) **cache-first**: `caches.match(request).then((cached) => cached || fetch(request))`. Once a device's service worker cached `/` a single time, every subsequent visit served that exact frozen response forever — the network was never consulted again for that URL on that device, regardless of how many new deploys shipped. Because `CACHE_NAME` was a static `"qarzdaftar-shell-v1"` and `sw.js`'s own bytes hadn't changed, the browser's SW update check (`skipWaiting`/`clients.claim()` were already present, so activation itself wasn't the blocker) never had a reason to re-run `install` and refresh the cached entry. This affected any returning device the moment it had ever cached `/` once — likely long before this session, meaning the user may have been on a stale bootstrap bundle for a while and this was the first time it visibly mattered (previous sessions' `/` changes were comparatively minor).

**Fix** (`frontend/public/sw.js`, commit `51abd48`): switched the shell fetch handler to network-first with cache used only as an offline fallback (`fetch(request).then(cache it, return it).catch(() => caches.match(request))`), and bumped `CACHE_NAME` to `"qarzdaftar-shell-v2"` so the `activate` handler's existing cleanup (`keys().filter(key => key !== CACHE_NAME).map(delete)`) purges the stale `v1` cache immediately on next load. This preserves CLAUDE.md §21's "offline app shell where practical" while guaranteeing every online visit gets the real current deploy.

**Not yet confirmed fixed** — pushed but the user hadn't retried again as of writing. Service worker activation isn't always instant on the very first reload after a SW-file change (the old SW may still control the page until its own update cycle completes), so **a full Telegram restart (not just re-opening the Mini App) or two consecutive reloads may be needed** before the fix visibly takes effect on an already-affected device. If the user reports it's still stuck after that, the next step is asking them to clear the site data / PWA cache for `qarzdaftar-tau.vercel.app` manually (Telegram Settings → Devices/clear cache, or Chrome Site Settings → Clear storage), since a sufficiently old cached SW registration could in theory predate today's fix too.

**Standing lesson for future sessions**: this bug was invisible to every verification method used all session — `npm run build`/`lint`, live `curl` against production, and even a real anonymous Chrome pass — because none of them go through a browser with an already-registered, already-populated service worker the way a returning real user's device does. `public/sw.js`'s caching strategy for any URL should default to network-first unless there's a specific, deliberate reason for cache-first (true static assets with content-hashed filenames are the normal exception — this app's Next.js build output already uses hashed asset URLs, so those don't need this fix). Re-check this file specifically after any future change to `/`, the home page, or the login flow.

### Follow-up UX request (same incident, same session): stop showing the marketing hero at all inside Telegram

While waiting to confirm the service-worker fix, the user asked to just remove the hero screen entirely so the Mini App lands directly in the real app — reasonable regardless of the caching bug, since even a correctly-working bootstrap still had one render pass showing marketing copy before redirecting.

- Replaced `frontend/src/components/telegram-bootstrap.tsx` (deleted) with `frontend/src/components/home-gate.tsx`, mounted from a simplified `app/page.tsx`. `HomeGate` determines Telegram-vs-browser via a `useState(isTelegramContext)` **lazy initializer** rather than discovering it inside a `useEffect` — safe because `telegram-web-app.js` loads with `strategy="beforeInteractive"` (already true, unchanged), so `window.Telegram.WebApp.initData` is guaranteed populated before this component hydrates. Inside Telegram: renders a bare `<main>` (nothing visible) while login runs, then `router.replace()`s away — the marketing hero literally never paints. Outside Telegram: falls straight through to the hero, matching previous behavior exactly. On login failure: shows the same visible red error box added earlier this incident (now on `HomeGate` instead of the deleted component).
- First lint pass caught a real `react-hooks/set-state-in-effect` violation from calling `setStatus(...)` synchronously in the effect body for the browser/Telegram determination — fixed by moving that determination into the lazy `useState` initializer instead (only the actual async login continuation calls `setState`, inside a `.then()`, which the rule doesn't flag).
- Verified: frontend `build`+`lint` clean, live `next dev` + `curl` confirmed `/` still returns 200 with the hero text present in the server-rendered HTML for a plain (non-Telegram) visitor — the only regression risk worth checking, since Telegram-context behavior can't be exercised outside a real Telegram client.
- **Committed and pushed as `61df9dc`** — confirmed by the user afterward that the Mini App opens correctly now. Both the service-worker fix (`51abd48`) and this hero-skip change (`61df9dc`) are live.

## First real admin bootstrapped (2026-08-29)

No admin account existed until this point — promoted the user's own Telegram-linked account ("Komronfrx", `@komronfrx`, id `fd7a1c50-af6a-4042-8f5c-33fef045d126`) to `role='admin'` directly in production Supabase (the documented Phase 9 bootstrap path: no self-serve promotion exists by design). Verified correct by cross-referencing `telegram_connections` (this row is the one actually linked to their real `telegram_id`, not a same-named decoy row — there were several similarly-named rows in `users` from real family/friends already using the app).

## Follow-up feature (same session): admin `/admin/users` now shop-focused, plus self-service phone number

User feedback after seeing the admin panel: the account-type label ("Личный"/"Shaxsiy" showing on every row) was noise, and they want each shop's Telegram username and phone visible for outreach without needing a separate channel.

- **Backend**: `AdminUserSummary` gained `telegramUsername: string | null` (from `telegram_connections.username`, previously only exposed as the `telegramConnected` boolean). `AdminService.listUsers()` now takes an optional `{ accountType?, search? }` filter — `GET /admin/users` **defaults to `accountType=business` only** (matches the actual admin workflow: monitoring shops, not scrolling every personal debt-tracking user); passing `?search=<text>` (sanitized against PostgREST filter-syntax special characters, matched via `ilike` on `name`/`phone`) drops the account-type filter so a personal account can still be found and converted. New `backend/src/admin/dto/list-users-query.dto.ts` validates the query params (required since the global `ValidationPipe` has `forbidNonWhitelisted: true`).
- **Backend, phone capture**: researched Telegram's `requestContact()` WebApp method before building anything (after two protocol-guessing bugs earlier the same day, this was checked against live docs first) — confirmed it does **not** return the phone number to frontend JS at all; the number is only delivered to the bot as a service message, which would need a whole new inbound Telegram Bot webhook receiver (nothing like that exists in this codebase yet — only outbound `sendMessage` and Mini App `initData` verification). Decided against building that infrastructure for this one field and instead added a plain self-service field: `PATCH /users/me/phone` (`backend/src/users/dto/update-phone.dto.ts`, regex-validated, `UsersService.updatePhone()`) — same trust level as the debt-counterparty phone field elsewhere in the app (self-reported, not Telegram-verified), which is an accepted honest tradeoff given the alternative is unbuilt infrastructure.
- **Frontend**: `app/(app)/profile/phone-editor.tsx` (new) — click-to-edit phone field on the profile page (replaces the previous plain read-only `{user.phone}` line), calls new `updatePhoneAction` in `profile/actions.ts` → `updateUserPhone()` in `lib/debts-api.ts`. `app/admin/users/page.tsx` rewritten: search form (`<form>` GET to `?q=`, no client JS needed) at the top; with no query, fetches `accountType=business` and shows only shops; with a query, fetches `search=` across all account types. Table dropped the account-type column entirely (the thing the user wanted removed) and instead shows business name + owner name stacked in one cell, real `@telegramUsername` instead of just a connected/not-connected boolean, and phone. `lib/admin-api.ts`'s `listAdminUsers()` now takes an optional filter object serialized to query params.
- **Live-verified against production** (same throwaway-script pattern as every other live check this session, deleted after): confirmed the business-only filter correctly excluded a freshly-created personal test account and included it only after conversion (with its real `telegramUsername`); confirmed `search` finds a personal account by partial phone digits without the business filter suppressing it; confirmed `PATCH /users/me/phone` persists and immediately reflects in the admin list; confirmed an invalid phone string is rejected with `400`. **Incidental finding, not a bug**: the business-only list already contained two real (non-test) accounts — "Комрон" and "Dinush🌗" — meaning the user had already tried the "do'konga aylantirish" flow themselves for real accounts before this follow-up request; left untouched, flagged to the user rather than assumed to be an error.
- Backend `build`+`lint`+`test` and frontend `build`+`lint` clean throughout; live `next dev` sweep of `/`, `/profile`, `/admin/users`, `/admin/users?q=test` all `200` with no dev-log errors.
- Committed and pushed as `130937d` (confirmed via `git log` at the start of the next session).

---

## Phase 13 — Per-shop subscription pricing, discount, and bonus days ✅ DONE, migrated and pushed to production

User request: shops (business accounts) pay a monthly subscription outside the app; the owner wanted a way to record each shop's monthly price, apply a discount some months, and grant bonus days — manageable from the admin panel, and visible to the shop owner in their own profile. Explicitly scoped as record-keeping only, consistent with Phase 12's "no in-app billing" decision: nothing here moves money or automates anything. `subscription_active` (the actual access gate checked by `JwtStrategy`) is untouched by any of this — an admin still flips it manually.

**Data model** — migration `supabase/migrations/0014_subscription_pricing.sql`: `users` gains `subscription_price` (numeric, default 0), `subscription_discount_percent` (numeric, default 0, `check` 0–100), `subscription_valid_until` (date, nullable — a "paid through" marker). **Applied to production this session** (`supabase db push` from the repo root; confirmed via `supabase migration list` showing local/remote matching through 0014).

**Backend** (`backend/src/admin/`):
- `admin.service.ts` — introduced a shared `USER_SUMMARY_COLUMNS` string constant (single literal, not built via `+` — concatenating literals widens the type to plain `string` and breaks supabase-js's typed-select inference, confirmed by hitting exactly that compile error while refactoring) and a `UserSummaryRow` type, replacing 4 previously-duplicated inline select-string/object-literal signatures. New `assertBusinessAccount()` private helper factors out the "must be a business account or 400" check previously inlined only in `updateSubscriptionStatus`.
- Two new methods: `updateSubscriptionPricing(targetUserId, {price?, discountPercent?})` (business-only, partial update — either field can be set independently) and `addSubscriptionBonusDays(targetUserId, days)` (business-only; extends `subscription_valid_until` from whichever is later, today or the shop's current valid-until date, so stacking bonus days on an account that's still paid up doesn't reset the clock to today).
- New DTOs: `update-subscription-pricing.dto.ts` (`price?`/`discountPercent?`, both `@IsNumber` with `@Min`/`@Max`), `add-subscription-bonus.dto.ts` (`days: number`, `@IsInt @Min(1)`).
- `admin.controller.ts` — `PATCH /admin/users/:id/subscription-pricing`, `POST /admin/users/:id/subscription-bonus`, both under the existing class-level `@Roles('admin')`.
- `AdminUserSummary` entity gained `subscriptionPrice`/`subscriptionDiscountPercent`/`subscriptionValidUntil` (all typed `string`/`string | null`, matching the existing convention for Postgres `numeric` columns elsewhere in this codebase — see `Debt.amount`).
- `database.types.ts` — added the 3 columns to `users` Row (as `string`, matching the numeric-as-string convention) and Insert (as `number`, matching how DTOs send them).
- `GET /users/me` needed no backend change — it already does `select('*')`, so the 3 new columns are included automatically once the migration is applied.
- Backend `npm run build` + `npm run lint` + `npm test` all clean.

**Frontend**:
- `lib/types.ts` — `CurrentUser` gained `account_type`/`subscription_price`/`subscription_discount_percent`/`subscription_valid_until` (this was actually a pre-existing gap from Phase 12 — `account_type`/`business_name`/`subscription_active` were never added to `CurrentUser` even though the backend already returned them; only `account_type` was added now, being the one this feature actually needs to conditionally show the subscription card). `AdminUserSummary` gained the matching 3 fields.
- `lib/admin-api.ts` — `updateAdminUserSubscriptionPricing()`, `addAdminUserSubscriptionBonus()`. `admin/users/actions.ts` — matching `updateSubscriptionPricingAction`/`addSubscriptionBonusAction` server actions (identical `getDictionary`+`extractApiErrorMessage` pattern as every other action in this file).
- `admin/users/business-status-control.tsx` — extended: business rows now show price (with discount % inline if any) and valid-until date, an "editSubscriptionPricing" toggle revealing price/discount inputs, and a bonus-days input + button. Gained a required `locale` prop (for `formatSom`/`formatDate`) — `admin/users/page.tsx` updated to pass it.
- `app/(app)/profile/page.tsx` — new "Obuna" card, rendered only when `user.account_type === "business"`: price, discount (only shown if >0), computed effective price (`price * (1 - discount/100)`), and valid-until date (or "muddat belgilanmagan" if null).
- New dictionary keys in both `uz.ts`/`ru.ts`: `admin.{subscriptionValidUntil,subscriptionNoExpiry,subscriptionPricePlaceholder,subscriptionDiscountPlaceholder,editSubscriptionPricing,addSubscriptionBonusDays}`, `profile.subscription.{title,price,discount,effectivePrice,validUntil,noExpiry}`.
- Frontend `npm run build` + `npm run lint` clean. Live `next dev` + `curl` sweep of `/`, `/profile`, `/admin/users` all `200` with no dev-log errors (per this project's own standing lesson that build/lint alone doesn't catch RSC runtime bugs) — though since this environment has no session cookie, this only proves no import/serialization crash, not the actual business-only render branches (those need a real logged-in business/admin session against a real backend, which only the user can do).

**Pushed to production** — commit `5db7fbc` on `main` (`130937d..5db7fbc`). Migration `0014` applied to production Supabase and code pushed to GitHub in the same session; Render (backend) and Vercel (frontend) both auto-deploy from `main`.

**Not done yet**: a real click-through as an admin (set a shop's price/discount, add bonus days) and as that shop's own owner (confirm the profile card shows the right numbers) — still owed, same standing gap as every other feature in this project's history; only verified via `npm run build`/`lint`/`test` and an anonymous `next dev` sweep (no session cookie, so the actual business-only render branches were never exercised).

### Bug found by the user immediately after deploy: saving pricing crashed with a masked 500

User tried the new "Narx/chegirmani tahrirlash" form on a real business account (typed a discount value containing a `%` character) and got `Serverda xatolik yuz berdi...` — the generic masked-500 text from `all-exceptions.filter.ts`, confirming a real backend crash, not a translation/display issue.

**Root cause, confirmed via an isolated `class-validator` test (no network/secrets needed)**: `Number("10%")` (or any non-numeric string) evaluates to `NaN`; `JSON.stringify({ discountPercent: NaN })` silently serializes that to `{"discountPercent":null}`. `UpdateSubscriptionPricingDto`'s fields used `@IsOptional()`, whose actual behavior (verified by reading `class-validator`'s source directly, not assumed) is to skip *all* validation when a value is `null` **or** `undefined` — so an explicit `null` sailed straight past `@IsNumber()`/`@Min()`/`@Max()` untouched. `admin.service.ts`'s `dto.price !== undefined` check is `true` for `null`, so `null` got written straight into `subscription_discount_percent`, a `not null` column — Postgres rejected it, `InternalServerErrorException` fired, and the global filter masked it to the generic string.

**Fix, two layers**:
- **Backend** (`update-subscription-pricing.dto.ts`): replaced `@IsOptional()` with `@ValidateIf((dto) => dto.field !== undefined)` on both `price`/`discountPercent` — this only skips validation when the field is genuinely absent (`undefined`), so an explicit `null` (or any other bad value) is now validated and rejected with a clean `400`, not silently passed through to crash the database write. Verified directly: an isolated `class-validator` `validate()` call (via a throwaway `ts-node` script, deleted after) confirmed `{price:100, discountPercent:null}` now REJECTED, while `{}`/`{discountPercent:10}`/normal values still VALID.
- **Frontend** (`business-status-control.tsx`): price/discount/bonus-days inputs switched from `type="text"` to `type="number"` (a browser-level number input cannot accept a literal `%` character at all — this alone would have prevented the exact bug that was hit), plus explicit JS validation before calling the server action (rejects non-finite/negative price, out-of-0–100-range discount, non-positive/non-integer bonus days) with a new localized inline error message (`dict.admin.subscriptionPricingInvalid`/`subscriptionBonusDaysInvalid` in both `uz.ts`/`ru.ts`) shown instead of ever reaching the network with bad input.
- Backend `build`+`lint`+`test` and frontend `build`+`lint` clean; live `next dev` sweep of `/admin/users` still `200`.
- No database cleanup needed — the failed save never committed (Postgres rejected the write outright), so the account's `subscription_price`/`subscription_discount_percent` were left at their prior values, not corrupted.
- **Pushed to production** as commit `fa8f3a2` — Render/Vercel auto-deploy from `main`.

### Follow-up (same day): dashboard promo banner so shops actually notice an active discount

User feedback: the discount was only visible if a shop owner thought to open their profile — asked for it to "stay visible" with a nicer UI specifically around the promotion itself.

- New `frontend/src/components/subscription-promo-banner.tsx` — async Server Component (reads its own locale/dict, same pattern as `sign-in-required.tsx`/`error-state.tsx`), renders **nothing** unless `account_type === "business"` **and** `subscription_discount_percent > 0` (a 0% discount isn't a promotion). Shows a `-X%` badge circle in warning-tint colors, the original price struck through next to the discounted price, matching the CLAUDE.md design system's warning color (`#F59E0B`) rather than introducing a new accent.
- Mounted on `app/(app)/dashboard/page.tsx` — the page shops actually land on — in both the empty-state and normal-state layouts, right below the greeting header (above the summary tiles), so it's the first thing a shop with an active discount sees on every visit, not something they have to go looking for in their profile.
- `app/(app)/profile/page.tsx`'s existing subscription card also restyled to match: the previously separate "Chegirma"/"Joriy narx" rows collapsed into one row (struck-through original price + bold discounted price) with a `-X%` pill badge next to the card title, consistent with the new dashboard banner instead of a plain 4-row `<dl>`.
- New dict keys: top-level `promoBanner.{title,perMonth}` in both `uz.ts`/`ru.ts`.
- Frontend `npm run build` + `npm run lint` clean; live `next dev` sweep of `/dashboard`/`/profile` both `200`, no dev-log errors (anonymous visitor only — no session, so the actual banner-rendering branch itself wasn't visually exercised, same standing gap as everything else business-account-related this session).
- **Pushed to production** as commit `fbdf430` — Render/Vercel auto-deploy from `main`.

### Follow-up (same day): diagnosed "discount didn't show", found a real gap, added a revert-to-personal endpoint

User reported the promo banner never appeared. Live diagnosis (throwaway service-role scripts against production, deleted after each — same established pattern as every prior session's live checks):
- Confirmed the 10% discount set on the "Saloh"/"sal" business account **did save correctly** (the earlier masked-500 fix from this session worked) — `price:100, discount:10, valid_until:"2026-09-28"`. The banner only renders for the account it's set on, and the user was checking a *different* account's own dashboard, so this part was working as designed, not a bug.
- The account the user was actually looking at ("Комрон"/business_name "asl") turned out to be **yet another, unrelated user row** (likely a stray counterparty placeholder from someone else's debt entry, not the user's real login) — mistakenly given a demo discount first, then reverted back to 0/0 once the mix-up was found.
- The user's **real** logged-in account (`username: komronfrx`, `role: admin`) turned out to have `account_type: "personal"` — never converted to business at all, which is the actual, correct reason the banner never showed for them specifically: the feature was working exactly as designed, personal accounts simply don't have a subscription/discount concept.
- User declined converting their real account to business for a live demo (reasonable — per Phase 12's own documented gap, there was **no way to undo** that via the admin panel). Rather than force a workaround, closed the actual gap: new `AdminService.revertToPersonal()` (resets `account_type`/`business_name`/`subscription_active`/`subscription_price`/`subscription_discount_percent`/`subscription_valid_until` all back to their defaults — not just flipping `account_type`, so a later re-conversion doesn't inherit stale pricing data), wired to `PATCH /admin/users/:id/personal`, with a matching frontend "Shaxsiy hisobga qaytarish" (red, click-to-confirm, mirroring the existing convert-to-business confirm pattern) button in `business-status-control.tsx`. `lib/admin-api.ts`'s `revertAdminUserToPersonal()` + `admin/users/actions.ts`'s `revertToPersonalAction()` follow the exact same pattern as every other action in that file. New dict keys `admin.{revertToPersonal,confirmRevertToPersonal}` in both `uz.ts`/`ru.ts`.
- Backend `build`+`lint`+`test` and frontend `build`+`lint` clean.
- **User's real account was deliberately left untouched** (still `personal`/`admin`) — they'll test the promo-banner flow themselves tomorrow using this new capability (convert their own account to business via the existing button, see the banner, then revert via this new button) rather than have it done for them.
- **Pushed to production** as commit `b46d9fa` — Render/Vercel auto-deploy from `main`.

---

## Phase 14 — Mini App made shop-only: self-serve subscription registration + checkout ✅ DONE, live-verified against production (2026-08-30)

Product pivot requested by the owner: instead of an admin manually onboarding every shop by hand, the Mini App itself now gates every route behind "business account with an active subscription" — a new user logging in via Telegram must register as a shop (name, optional phone, plan) and pay before they can use anything. Fixed plans: 1 month = 50 000 so'm, 2 months = 85 000 so'm (a 15% bundle discount, computed and stored the same way admin-set discounts already were in Phase 13, so the existing dashboard promo banner just works for it). Payment can happen two ways, per the owner's explicit request: in-app via the existing Click/Payme/Yagona Pay architecture, or the shop contacts support, who activates their account manually after taking payment outside the app (the same admin controls Phase 12/13 already built).

**Data model** — migration `supabase/migrations/0015_subscription_self_serve.sql`: `users` gains `access_override` (boolean, default false — an admin-only manual exemption from the whole gate, e.g. for a pre-existing personal account) and `subscription_plan_months` (integer, check 1 or 2). New `subscription_transactions` table, structurally identical to `payment_transactions` (migration 0008) but for subscribing to the platform itself rather than paying off a debt. Applied to production this session (`supabase db push`, confirmed via `supabase migration list` matching through 0015).

**Backend — the gate itself**:
- The blocking decision moved out of `JwtStrategy.validate()` (which now just fetches and attaches `accountType`/`subscriptionActive`/`accessOverride` to `AuthenticatedUser`, no longer throws) into a new global `SubscriptionGateGuard` (`backend/src/common/guards/subscription-gate.guard.ts`, registered in `app.module.ts` after `RolesGuard`). This split was necessary because a Passport strategy has no per-route escape hatch, and specific routes — most importantly the self-serve registration/checkout endpoints a blocked user must still reach — need one via a new `@SkipSubscriptionGate()` decorator (mirrors `@Public()`'s `Reflector` pattern exactly). Admins are always exempt (same self-lockout guard as Phase 12); everyone else needs `accountType==='business' && subscriptionActive`, or `accessOverride===true`, else a `403` with code `ACCESS_RESTRICTED` (not yet a business) or `SUBSCRIPTION_INACTIVE` (business but unpaid/deactivated — reused from Phase 12).
- `GET /users/me`, `PATCH /users/me/business` (new), and `POST /subscription-payments/:provider/checkout` (new) are all `@SkipSubscriptionGate()`'d — the minimum set of routes a blocked user needs to see their own state and become unblocked.

**Backend — self-serve registration**: new `backend/src/common/subscription-plans.ts` (`SUBSCRIPTION_PLAN_PRICES`, `subscriptionPlanDiscountPercent()` — fixed constants, not an admin-editable settings table, since nothing in CLAUDE.md specifies one and building one with nothing else behind it would be exactly the "fake settings screen" section 43 warns against). New `RegisterBusinessDto` (`businessName`, `planMonths`, optional `phone` — see phone note below) and `UsersService.registerBusiness()` (`PATCH /users/me/business`), which sets `account_type='business'` + plan/price/discount and **always explicitly forces `subscription_active: false`** — this had to be explicit, not just "never touched", because `users.subscription_active` defaults to `true` at the database level (migration 0013), a default that only ever mattered for personal accounts before (where the gate ignored it) and would otherwise let a freshly-registered business inherit `true` and get free access. **Found and fixed live** during this session's own verification pass (see below) — caught before any real user could exploit it, but it was a real, shippable bug.

**Backend — subscription checkout/webhook** (new `backend/src/subscription-payments/` module): `SubscriptionPaymentsService.initiateCheckout()` reads plan/price off the user's own row (never client-supplied, same principle as debt-payment checkout using the server-computed remaining amount) and throws `NOT_REGISTERED_AS_BUSINESS` if they haven't registered/picked a plan yet. `handleWebhook()` duplicates `PaymentTransactionsService`'s check/create/cancel/complete phase state machine (Click Prepare/Complete, Payme's 4 methods) on `subscription_transactions` rather than generalizing the existing debt-payment service to handle both — deliberate, to avoid touching the already-tested, already-live debt payment flow for an unrelated feature. The terminal phase activates the subscription (`subscription_active=true`, `subscription_valid_until` extended by `plan_months` from whichever is later, today or the current valid-until — same "stack, don't reset" rule as `AdminService.addSubscriptionBonusDays`). Reused the **exact same** `ClickProvider`/`PaymeProvider`/`YagonaPayProvider`/`PaymentProvidersService` instances as debt payments (`payment-providers.module.ts` now also exports `PaymentProvidersService`, not just `PaymentTransactionsService`) — `CheckoutRequest.debtId` was confirmed unused by all 3 provider implementations before being made optional, so this required no provider-level changes at all. New `SubscriptionExpiryService` (`@Cron(CronExpression.EVERY_DAY_AT_3AM)`) auto-deactivates business accounts past `subscription_valid_until`, closing the loop on "buy N months" actually meaning N months rather than permanent access — only ever touches `subscription_active`, never `access_override` or admin-granted unlimited access (no `valid_until` set).

**Backend — admin `access_override`**: `AdminService.setAccessOverride()` / `PATCH /admin/users/:id/access-override`, same pattern as every other admin user-mutation endpoint in this file.

**Backend — phone capture at registration** (follow-up request, same session): a self-registering shop's phone wasn't visible to the admin until Phase 12-era `/profile` self-service phone field — except a blocked user can never reach `/profile` (it's behind the same gate). `RegisterBusinessDto` gained an optional `phone` (same regex as the existing `UpdatePhoneDto`), so the admin panel has real outreach info (phone + Telegram username, the latter already automatic via `telegram_connections` since Telegram login always links it) from the moment a shop registers, before they've ever paid.

**Frontend**:
- `CurrentUser` gained `business_name`/`subscription_active`/`subscription_plan_months`/`access_override` (a pre-existing gap since Phase 13 — the backend already returned these via `select('*')`, the frontend type just never had them).
- `app/(app)/layout.tsx` rewritten: previously detected a blocked user by catching a `403` from `getCurrentUser()` and sniffing its error code — that stopped working the moment `GET /users/me` became gate-exempt (it never throws for a blocked user anymore), so the layout now reads `account_type`/`subscription_active`/`access_override` directly off the always-succeeding response, exactly mirroring what the backend guard itself checks. Simpler and more direct than the old approach, and it also hands the gate screen the data it needs (business name, plan, price) without a second fetch.
- New `components/subscription-gate.tsx` (replaces the deleted static `subscription-inactive.tsx`) — two states: not-yet-business renders `business-register-form.tsx` (name + optional phone + 1/2-month plan picker with live price/discount, `type="number"`-equivalent care not needed here since it's a pure pick-one UI); business-but-unpaid renders the chosen plan summary + `subscription-checkout-picker.tsx` (thin wrapper reusing Phase 11's existing `PaymentMethodPicker` component — same Click/Payme/Yagona Pay cards, zero new UI needed there) + a support-contact hint for the cash/outside-app path. New `app/(app)/subscribe-actions.ts` (`registerBusinessAction`, `initiateSubscriptionCheckoutAction`) follows the established `getDictionary`+`extractApiErrorMessage` server-action pattern used everywhere else in this codebase.
- `components/subscription-promo-banner.tsx` tightened: now also requires `subscription_valid_until` to still be in the future (not just `discount_percent > 0`) — so a 2-month plan's bundle discount only advertises itself for those 2 months, not forever, without needing a separate cron to clear the discount field once the period ends.
- Admin panel UX pass (user feedback: "sal noqulay" / a bit inconvenient to use): `admin/users/page.tsx` rewritten from a cramped horizontally-scrolling `<table>` (7 columns, several stacked buttons crammed into one narrow cell) to a card-per-user list (`rounded-2xl bg-card shadow-sm`, consistent with the rest of the app's design system), grouping identity info, role control, the new access-override toggle, and business/subscription controls into clear sections. New `access-override-toggle.tsx` (mirrors `role-toggle-button.tsx`'s pattern exactly). Cleaned up 6 dictionary keys (`tableName`/`tableRole`/`tableCreatedAt`/`tableAccountType`/`accountTypePersonal`/`accountTypeBusiness`) that were already dead (the account-type table column had been dropped in an earlier session) or became dead from this rewrite.
- New dict sections in both `uz.ts`/`ru.ts`: `subscriptionGate.*` (replaces the deleted `subscriptionInactive.*`), `admin.accessOverride*`, `apiErrors.ACCESS_RESTRICTED`/`NOT_REGISTERED_AS_BUSINESS`.

**Live-verified against production** (throwaway test users via a service-role script, deleted after — same established pattern as every prior session's live checks): confirmed the full gate state machine end-to-end over real HTTP calls against the compiled backend —personal account blocked with `ACCESS_RESTRICTED` → registers as business (plan=2, price=85 000, discount=15%) → **first run caught the `subscription_active` default-true bug live** (business-but-unpaid incorrectly got `200` instead of `403 SUBSCRIPTION_INACTIVE`) → fixed (`subscription_active: false` forced explicitly in `registerBusiness`) → re-ran, now correctly `403 SUBSCRIPTION_INACTIVE` → simulated a verified webhook activating the account → `200`. Separately confirmed `access_override=true` on a personal account bypasses the gate entirely, and the checkout endpoint's two honest failure paths (`NOT_REGISTERED_AS_BUSINESS` before registering, `PROVIDER_NOT_CONFIGURED` after — no real Click/Payme/Yagona Pay credentials exist yet, same as every prior payment-provider session). Backend `build`+`lint`+`test` and frontend `build`+`lint` clean throughout; live `next dev` sweep of `/`, `/dashboard`, `/admin/users`, `/profile`, `/activity`, `/statistics`, `/debts` all `200` with zero errors in the dev log (anonymous visitor, per the project's standing lesson that build/lint alone doesn't catch RSC runtime bugs).

**Production data reset** (explicit, confirmed user request, same session): all 19 non-admin users — and everything that cascaded from them (debts, payments, receipts, reminders, telegram_connections, push_subscriptions, notifications) — were permanently deleted from production Supabase, leaving only the owner's own admin account. This was a deliberate, irreversible action taken only after explicit confirmation (asked via a direct yes/no question given the severity), not something done automatically. Verified live afterward: `debts`/`payments`/`receipts` all `0` rows, `users` down to `1`.

**Deliberately out of scope / known gaps, same honesty pattern as every prior payment-provider session**:
- No real Click/Payme/Yagona Pay credentials exist yet — in-app subscription checkout correctly shows "not configured" until the owner supplies them, exactly like debt-payment checkout has since Phase 11.
- The subscription webhook handler was **not** exercised against a self-issued fake-secret simulation this session (unlike the Phase-11-hardening session's thorough Click/Payme protocol replay) — it's a close structural mirror of `PaymentTransactionsService`, which *was* thoroughly simulated, but this specific new code path's byte-level correctness against each real provider is still unverified. Worth a dedicated simulation pass before real credentials are ever configured.
- No real end-to-end click-through (a real person opening the Mini App fresh, registering as a shop, paying) has happened yet — everything this session was verified via direct HTTP calls with synthetic test users, not a real Telegram session.
- A stray, unexplained cosmetic issue was noticed and is unrelated to this session's code: running a throwaway Node script printed `◇ injected env (13) from .env // tip: ⌁ auth for agents [www.vestauth.com]` — investigated and confirmed to be the real, official `dotenv` package (v17.4.1, legitimately installed) printing a self-promotional tip message on `config()`, documented in its own README/CHANGELOG. Not a security issue, just an unexpected marketing string from an otherwise-trusted dependency; noted here in case it causes confusion in a future session's logs.

**Pushed to production** — commit `0242841` on `main`. Migration `0015` applied to production Supabase and code pushed to GitHub in the same session; Render (backend) and Vercel (frontend) both auto-deploy from `main`.

### Follow-up (same session): "contact support" turned into a real action

User feedback: the pending-payment screen's "contact support" line was just text — no actual button/action behind it. Migration `0016_cash_payment_request.sql` adds `users.cash_payment_requested_at` (nullable timestamptz). New `SubscriptionPaymentsService.requestCashPayment()` (`POST /subscription-payments/cash-request`, `@SkipSubscriptionGate()`): sets the timestamp and notifies every `role='admin'` user, both an in-app `notifications` row and a Telegram message (gated on `telegram_enabled`, same pattern as `PaymentTransactionsService.notifyLender`). Cleared automatically the moment an admin activates the subscription (`AdminService.updateSubscriptionStatus(active=true)` and `SubscriptionPaymentsService.activateSubscription()` both null it out); `AdminService.revertToPersonal()` also clears it as part of its full-reset pattern (and, while touching that method, also now resets `subscription_plan_months` to null — a small pre-existing gap in that method's "reset every subscription field" intent). New `components/cash-payment-request-button.tsx` replaces the static hint text on `subscription-gate.tsx`'s pending screen; `admin/users/page.tsx` shows a persistent warning-colored badge with the request timestamp on any shop row that has one. Live-verified end-to-end against production with a throwaway test shop (correctly rejects before registration with `NOT_REGISTERED_AS_BUSINESS`, flags + creates a real notification row for the actual admin account on request, idempotent on repeat, clears on simulated activation). Backend `build`+`lint`+`test` and frontend `build`+`lint` clean. **Pushed to production** as commit `d5b58c1`.
